# 2026-05-04 工作日志

## 项目信息
- 项目: QuantDinger (A股量化交易系统)
- 上游仓库: https://github.com/brokermr810/QuantDinger
- 分支仓库: https://github.com/yuguonet/QuantDinger
- 本地路径: /root/.openclaw/workspace/QuantDinger-main/
- 后端代码: backend_api_python/app/ (~103,000 行 Python)

## 约束条件
1. 需考虑上游主版本升级兼容
2. 仅使用国内免费 A 股数据源 (腾讯/新浪/东财/AkShare)
3. 国内部署无法访问国外源 (TwelveData/yfinance/Finnhub)
4. 免费源不稳定，需加强实时性、准确性、有效性
5. 需减少 API 访问量 + 提高访问速度

---

## 架构分析 (已完成)

### 项目结构
```
app/
├── __init__.py              # Flask app factory + 全局单例
├── config/                  # 配置层
├── data_sources/            # 数据获取层 (原始数据拉取 + 多源 fallback)
├── interfaces/              # A股接口层 (对 data_sources 的门面封装)
├── data_providers/          # 国际数据层 (独立于 data_sources)
├── market_cn/               # A股分析层 (恐贪指数/板块/宏观)
├── market_store/            # 本地存储层 (Pickle/Feather)
├── services/                # 业务服务层
├── routes/                  # 路由层 (Flask Blueprint)
├── agent/                   # AI Agent 模块
└── utils/                   # 工具库
```

### 数据获取路径 (已验证)

**A 股有 5 条独立获取路径**:

| 路径 | 入口 | 数据类型 | 数据源 | 使用者 |
|------|------|----------|--------|--------|
| A | DataSourceFactory → AStockDataSource | 实时行情/K线/涨跌停池/龙虎榜 | 腾讯→新浪→AkShare→东财→TwelveData | services/* |
| B | AShareDataHub → Interface → AStockDataSource | 同路径 A (门面封装) | 同路径 A | routes/shichang.py |
| C | market_cn/china_stock.py → ChinaData | 宏观经济 + 历史指数 + 北向资金 | Tushare→AKShare→BaoStock | fear_greed_index |
| D | data_providers/sentiment.py | 国际情绪 (VIX/DXY) | 独立 HTTP | routes/shichang.py |
| E | market_cn/hot_sectors.py | 板块热度 | 直接 HTTP 东方财富 | sector_history |

**关键发现**: china_stock.py 与 a_stock.py 是**互补关系**，不是重复
- a_stock.py = 实时市场数据 (行情/涨跌停池/龙虎榜)
- china_stock.py = 宏观经济数据 (GDP/CPI/PMI等) + 历史数据

### 缓存系统 (4~5 套独立实现)

| 缓存系统 | 位置 | 格式 | 使用者 |
|----------|------|------|--------|
| CacheManager | utils/cache.py | Redis/内存 | data_sources, data_providers |
| cache_manager.py | data_sources/cache_manager.py | 内存 | data_sources 内部 |
| cache_file.py | interfaces/cache_file.py | Feather 文件 | interfaces |
| Pickle 文件缓存 | market_cn/china_market.py | Pickle | market_cn |
| MarketStore | market_store/market_store.py | Pickle | market_store |

### 核心调用链 (已验证)

**K 线数据流**:
```
routes/kline.py → KlineService → DataSourceFactory.get_source(market)
  └── CNStockDataSource.get_kline() → _build_kline_sources() → fallback:
      1. tencent_kline (日/周线)     ✅ 快, 免费
      2. tencent_minute_kline (分钟)  ✅ 快, 免费
      3. sina_minute_kline (分钟)     ✅ 免费, 偶尔不稳
      4. sina_kline (日线)            ✅ 免费
      5. akshare_minute (分钟)        ✅ 较慢
      6. akshare_weekly (周线)        ✅ 较慢
      7. eastmoney_kline (全周期)     ✅ 快, 免费
      8. twelvedata (海外付费)        ❌ 无法访问
```

**实时行情流**:
```
CNStockDataSource.get_ticker() → fallback:
  1. tencent (腾讯 qt.gtimg.cn)    ✅ 快, 免费
  2. eastmoney (东财 push2)        ✅ 快, 免费
  3. sina (新浪 hq.sinajs.cn)      ✅ 免费
```

**回测数据流**:
```
routes/backtest.py → BacktestService → DataSourceFactory.get_source(market).get_kline()
  → IndicatorParamsParser + IndicatorCaller → 结果写入 DB
```

**实时交易数据流**:
```
routes/strategy.py → TradingExecutor.start_strategy()
  → 策略线程: KlineService → IndicatorCaller → 写入 pending_orders
    → PendingOrderWorker: 轮询 pending_orders → live_trading/* 下单
```

**快速分析数据流**:
```
routes/fast_analysis.py → FastAnalysisService → MarketDataCollector
  → DataSourceFactory (K线) + data_providers (宏观) + Finnhub (新闻) + akshare (基本面)
```

**A 股市场看板**:
```
routes/shichang.py
  /api/shichang/overview → DataSourceFactory → AStockDataSource (涨跌停池/龙虎榜/热榜)
  /api/shichang/fear-greed → fear_greed_index → china_stock.py (指数日线/全A行情/北向资金)
  /api/shichang/hot-sectors → hot_sectors.py → 直接 HTTP 东方财富 API
  /api/shichang/macro → china_market.py → china_stock.py (GDP/CPI/PMI等)
  /api/global-market/sentiment → data_providers.sentiment (VIX/DXY, 独立HTTP)
```

---

## 问题清单 (已验证)

| # | 层级 | 问题 | 严重度 |
|---|------|------|--------|
| 1 | 缓存 | 缓存系统碎片化 (4~5 套独立实现) | P0 |
| 2 | 数据获取 | hot_sectors.py 绕过数据源层直接 HTTP | P0 |
| 3 | 数据获取 | data_providers/ 与 data_sources/ 架构不统一 | P1 (海外源, 不动) |
| 4 | 服务层 | 巨型文件: backtest(4937) trading_executor(3862) fast_analysis(3204) pending_order(2451) collector(2356) | P1 |
| 5 | 架构 | 全局单例散落 (__init__.py + Factory + 各模块) | P1 |
| 6 | 命名 | xuangu.py / shichang.py 用拼音 | P2 |
| 7 | 路由 | quick_trade.py 直接调用 live_trading 绕过 services | P2 |
| 8 | 数据获取 | market_cn/ 自建 fallback 未复用 data_sources/ | P2 |
| 9 | 路由 | strategy.py(2028) user.py(1948) 过厚 | P2 |

---

## 重构计划 (REFACTOR_PLAN.md)

### Phase 1: 数据源层优化 (核心) 🔨
解决问题: #2, #8

- Step 1.1: 移除 A 股链中的海外源 (TwelveData)
- Step 1.2: 优化 K 线 fallback 顺序 (东财从第7提升到第2)
- Step 1.3: 增强熔断器策略 (冷却期 3→5 分钟, 半开 1→2 次试探)
- Step 1.4: 增强 rate_limiter (各源独立 QPS)
- Step 1.5: 规范化 market_cn/ 数据获取

### Phase 2: 智能缓存体系 (关键) 🧠
解决问题: #1

- Step 2.1: 交易时段感知缓存 (非交易时段 TTL 延长 60 倍)
- Step 2.2: 熔断时缓存兜底 (DataCache.get_stale())
- Step 2.3: 预热缓存 (开盘前自动加载)
- Step 2.4: 统一缓存层 (4~5 套 → 2 套)

### Phase 3: 批量请求优化 ⚡
- Step 3.1: 扩展批量 K 线获取 (并行)
- Step 3.2: 批量实时行情优化
- Step 3.3: 并行 fallback (关键数据)

### Phase 4: 依赖治理 + 单例管理 🏗️
解决问题: #5

- Step 4.1: 创建 app/core/registry.py — 统一单例注册表
- Step 4.2: 消除 __init__.py 中的全局变量
- Step 4.3: DataSourceFactory 单例治理

### Phase 5: 服务层拆分 ✂️
解决问题: #4

- Step 5.1: backtest.py (4937行) → 5 个子模块
- Step 5.2: trading_executor.py (3862行) → 4 个子模块
- Step 5.3: fast_analysis.py (3204行) → 按分析类型拆分
- Step 5.4: pending_order_worker.py (2451行) → 分离扫描/状态机/通知
- Step 5.5: market_data_collector.py (2356行) → 按数据源拆分

### Phase 6: 路由瘦身 + 命名统一 🚀
解决问题: #6, #7, #9

- Step 6.1: 瘦身路由 (strategy/user/quick_trade)
- Step 6.2: 统一命名 (xuangu→stock_picker, shichang→market_dashboard)
- Step 6.3: 统一错误处理 (routes/error_handler.py)

### 执行顺序建议
Phase 1 → Phase 2.1-2.2 → Phase 3 → Phase 4 → Phase 2.3-2.4 → Phase 5 → Phase 6

### 上游兼容策略
- 海外源不硬删除，用 ALLOW_OVERSEAS_SOURCES 配置跳过
- 拆分文件保留原文件作 facade
- 新增文件不影响上游 (trading_hours.py, cache_warmer.py, registry.py)

---

## 重构计划调整

### Phase 1 评估结果 (2026-05-04 13:17)
重新评估 Phase 1 各 Step 实际价值:
- Step 1.1 移除海外源: **跳过** — TwelveData 国内可访问，移除无意义
- Step 1.2 调整 fallback 顺序: **降优先级** — 串行 fallback，第一个成功就返回，顺序影响有限
- Step 1.3 增强熔断器: 保留
- Step 1.4 增强限流: 保留
- Step 1.5 规范化 market_cn: 降优先级

**结论**: Phase 1 对架构和速度提升有限。真正有架构价值的是 Phase 2 (智能缓存)。

### 决定: 从 Phase 2.1 (交易时段感知缓存) 开始

---

## Phase 2.1 分析: CNStockDataSource 缓存改造

### 文件信息
- 路径: `backend_api_python/app/data_sources/cn_stock.py`
- 行数: 726 行
- 职责: A 股数据核心入口，多源 fallback + 超时 + 熔断

### 当前缓存机制 (硬编码)

| 方法 | 缓存 key | TTL | 位置 |
|------|----------|-----|------|
| `get_ticker()` | `ticker:{code}` | 600s (10分) | L346 |
| `get_kline()` | `generate_kline_cache_key(...)` | 日线 300s / 分钟 120s | L399-400 |
| `get_kline_batch()` | 复用 kline_cache | 同上 | 继承 |

### 缓存问题
1. TTL 全部硬编码 — 交易时段和非交易时段用同一个 TTL
2. 非交易时段浪费 — 收盘后每 10 分钟还会重新请求行情数据
3. 熔断无兜底 — 所有源失败直接返回 `[]`，不尝试过期缓存
4. `get_kline_batch()` 无缓存检查 — 批量获取时不检查交易时段

### 数据源 fallback 链 (当前)
```
K线日/周: 腾讯 fqkline → 腾讯 mkline(分钟) → 新浪(分钟) → 新浪(日) → AkShare → 东财 → TwelveData
实时行情: 腾讯 qt.gtimg.cn → 新浪 hq.sinajs.cn → 东财 push2
```

### 核心方法分析

**get_ticker() (L330-360)**:
- 先查 realtime_cache → 缓存命中直接返回
- fallback: tencent_quote → sina_quote → eastmoney_quote
- 成功后写缓存 ttl=600s (硬编码)
- 所有源失败返回 `{"last": 0, "symbol": code}`

**get_kline() (L368-410)**:
- 先查 kline_cache → 缓存命中直接返回
- 调用 _build_kline_sources() 构建 fallback 列表
- 逐源尝试，_validate_kline_result() 校验
- 成功后写缓存: 日线 ttl=300s, 分钟 ttl=120s (硬编码)
- 所有源失败返回 `[]`

**get_kline_batch() (L412-530)**:
- 日线批量优化: 腾讯批量行情 → 新浪 fallback → 东财 fallback
- 缓存命中: 从 kline_cache 读 + 拼当日行情
- 缺失历史: 串行调 get_kline() 补数据
- 月线: 走日线批量 + 聚合

**_build_kline_sources() (L639-730)**:
- 构建 fallback 列表 (腾讯→新浪→AkShare→东财→TwelveData)
- 按周期选择数据源

### Phase 2.1 改动方案

**需要改 3 个文件，新增 1 个文件**:

| 文件 | 动作 | 改动量 | 内容 |
|------|------|--------|------|
| `app/data_sources/trading_hours.py` | **新增** | ~80 行 | 交易时段判断工具 |
| `app/data_sources/cache_manager.py` | 修改 | ~10 行 | DataCache 增加 get_stale() 方法 |
| `app/data_sources/cn_stock.py` | 修改 | ~30 行 | 使用动态 TTL + 熔断缓存兜底 |
| `app/data_sources/base.py` | 可选 | ~10 行 | 基类加入默认 TTL 配置 |

**trading_hours.py 设计**:
```python
def get_market_state() -> str:
    """返回 "trading" / "pre_market" / "post_market" / "closed_day" """
    # 交易时段: 09:15-11:30, 13:00-15:00
    # 非交易日: 周末 + 节假日 (复用 trading_calendar.py)

def get_ttl(data_type: str) -> int:
    """根据当前市场状态返回合适的 TTL"""
    # realtime_quote: trading=15s, non_trading=3600s, closed_day=86400s
    # kline_intraday: trading=30s, non_trading=7200s, closed_day=86400s
    # kline_daily:    trading=300s, non_trading=86400s
```

**cn_stock.py 改动点**:
- L346: `ttl=600` → `ttl=get_ttl("realtime_quote")`
- L399-400: 静态 ttl → `get_ttl("kline_daily" / "kline_intraday")`
- get_kline() 末尾: 增加 stale cache 兜底

**cache_manager.py 改动**:
```python
def get_stale(self, key: str, max_age: float) -> Optional[Any]:
    """返回过期但未淘汰的数据 (熔断兜底用)"""
    entry = self._cache.get(key)
    if entry and (time.time() - entry.timestamp) < max_age:
        return entry.data
    return None
```

### 预期效果
- 非交易时段 API 调用减少 90%+ (TTL 从 10 分钟 → 1 小时 / 24 小时)
- 熔断时返回过期缓存而非空结果
- 交易时段保持高频更新 (15-30 秒)

---

## 输出文件清单
- ARCHITECTURE_ANALYSIS.md — 初版分析 (已废弃，保留参考)
- ARCHITECTURE_DETAIL.md — 复核版详细分析 (含逐行验证的 6 条核心调用链)
- REFACTOR_PLAN.md — 最终重构计划 (执行依据)
- memory/2026-05-04.md — 本日志

## 当前状态
**位置**: Phase 2.1 — CNStockDataSource 缓存改造，分析完成，待执行
**下一步**: 新增 `trading_hours.py`，然后修改 `cache_manager.py` 和 `cn_stock.py`
