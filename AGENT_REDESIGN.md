# Agent 架构重设计 — 分项记分制

> 日期: 2026-06-07
> 状态: 设计完成，待实施
> 关联项目: https://github.com/yuguonet/QuantDinger

## 一、问题诊断

### 核心问题：相同输入，不同输出
同样时间、同样个股、同样语句，5次运行出现3种不同结论（买入/卖出/hold）。

### 根因分析（6层）
1. **LLM温度太高** — temperature=0.7，采样路径不确定
2. **Agent无决策框架** — LLM自由判断，无硬性规则约束
3. **数据缺失时LLM自由发挥** — 缺数据靠想象填补
4. **Agent类型自动切换** — CodeAgent vs ToolCallingAgent 行为不同
5. **上下文压缩引入噪声** — 上轮方向性结论污染本轮
6. **意图路由可能分流** — 不同domain走不同分析路径

### 架构层面的根本缺陷
- 当前架构：**LLM自由推理** — LLM决定调什么工具、调多少次、怎么组合、给什么结论
- 问题：所有不确定性都从LLM采样来，无法保证一致性
- 历史优先级设计有误：政策(最高)→游资→概念→动量→技术(最低)
  - 优先级意味着"上面的缺了下面的不敢信"
  - 重大政策99%的交易日没有，但每次都优先判断，浪费步骤
  - 龙虎榜盘后才有，游资一日游，追买=接盘

## 二、新架构：分项记分制

### 核心哲学
1. **价格折扣一切**（Dow Theory）— 所有信息最终反映在价格上
2. **能回测验证的才是可靠的** — K线/技术指标可回测，新闻/政策不可回测
3. **分项独立打分，无优先级** — 每项独立贡献，缺了就是0分，不影响其他项

### 设计规则

```
每个子项: 0-100 分（正常），-1000 分（极度偏差/一票否决）
最终得分: 各子项分数 × 权重，加权求和
权重迭代: 用实际结果(T+1/T+2涨跌)验证，自动调整权重
否决机制: -1000 在体系内，不用额外层
```

### 示例
```
K线形态:    75 × 0.30 = 22.5
MACD:       80 × 0.15 = 12.0
RSI:        60 × 0.10 =  6.0
成交量:     70 × 0.15 = 10.5
板块联动:   65 × 0.10 =  6.5
消息面:   -100 × 0.20 = -20.0  ← 极度偏差，一票否决
──────────────────────────────
总分: 37.5 → 不做
```

### 架构优势
- **无优先级** — 不依赖任何单一数据源
- **可回测** — 每个子项可单独验证胜率
- **可迭代** — 只调权重，不改架构
- **可审计** — 用户看到分数明细就知道为什么买/卖
- **一票否决内建** — -100就是否决，杀伤力由权重决定
- **缺数据不致命** — 缺了就是0分，其他项正常工作

## 三、分析层次

```
市场分析（大盘/板块/宏观）   ← 锦上添花的花
个股分析（基本面/消息/资金）  ← 锦上添花
K线技术分析（价格/量/指标）  ← 蛋糕本身
```

- K线技术分析是**唯一能回测验证**的地基
- 个股分析用来验证和增强信心
- 市场分析用来解释异常

## 四、数据陷阱警告

| 数据源 | 陷阱 | 正确用法 |
|--------|------|----------|
| 龙虎榜 | 盘后公布，游资一日游 | 风险警示，不是买入信号 |
| 资金流向 | 滞后，主力可对倒 | 验证工具，不单独决策 |
| 新闻/舆情 | 你看到时市场已反应 | 解释工具，不预测方向 |
| 北向资金 | 有时滞 | 连续大幅流入/流出才值得关注 |
| 解禁/减持 | 提前公布，市场可能已消化 | 中长线风险，短线可忽略 |

## 五、待实施的代码改动

### 已完成的临时改动（需回退或重构）
- `model.py` — temperature 从 0.3 降到 0.05 ✅ 保留
- `agent.py` — 强制CodeAgent类型 ✅ 保留
- `agent.py` — 数据完整性预检 ✅ 保留
- `agent.py` — prompt规则9/10（数据完整性+确定性）✅ 保留
- `calibration.py` — 客观评分校准层 → **需重构为分项记分制**
- `guidance.py` — 数据优先框架 + 数据陷阱警告 ✅ 保留
- `context_compressor.py` — 方向性关键词净化 ✅ 保留

### 需要新建
1. **`scoring/`** — 分项记分制核心
   - `items.py` — 子项定义（每项0-100打分函数）
   - `engine.py` — 记分引擎（加权求和）
   - `weights.py` — 权重管理（持久化+迭代）
   - `feedback.py` — 反馈闭环（T+1验证→权重调整）
   - `veto.py` — -100否决检测

2. **`scoring/items/`** — 各子项打分实现
   - `kline_pattern.py` — K线形态（涨停/连板/断板/缩量回调...）
   - `macd.py` — MACD信号
   - `rsi.py` — RSI信号
   - `kdj.py` — KDJ信号
   - `boll.py` — 布林带信号
   - `ma.py` — 均线信号
   - `volume.py` — 量价关系
   - `fund_flow.py` — 资金流向
   - `sector.py` — 板块联动
   - `news.py` — 新闻/消息面（含-100否决）
   - `policy.py` — 政策面（含-100否决）
   - `dragon_tiger.py` — 龙虎榜（风险警示为主）

### 需要修改
- `agent.py` — Agent不再自由推理，改为执行记分流程
- `calibration.py` — 替换为记分制引擎
- `guidance.py` — 更新为记分制规则

## 六、反馈闭环设计

```
T日: 记分 → 总分 → 决策(买入/卖出/观望) → 记录
T+1/T+2: 获取实际涨跌 → 对比决策
  → 正确: 权重保持
  → 错误: 分析哪项偏差最大 → 调整该项权重
周期: 每周/每月批量校准一次
```

## 七、与现有系统的关系

- **保留**: tools/（全部工具）、session_store、data_sources/、LLM调用
- **替换**: Agent推理逻辑+记分制引擎

