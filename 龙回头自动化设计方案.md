# 龙回头Pro 自动化接入设计方案 v1.1（已拍板稿）

> 版本: v1.1 ｜ 日期: 2026-09-03 深夜
> 相对 v0.9 的关键变化：**放弃虚拟组方案，改为 qd_watchlist 真分组 + 表结构改造**（用户授权改表/改约束）；confirm 模式淘汰（仅回测对照）；9:26 集合竞价快照纳入开盘判定；14:30 预确认确认要做；单用户（user_id=1）全用户可见。

## 0. 拍板结论

| # | 决策 | 结论 |
|---|---|---|
| 1 | 入场模式 | **d1open 唯一线上模式**（D1开盘按gap区间买 + D1收盘确认分类处置）。confirm 模式胜率无参考意义，仅保留在 test_dragon.py 作回测对照，不做自动化 |
| 2 | 多用户 | 不考虑。信号写 user_id=1（默认用户），所有用户可见同一策略组 |
| 3 | 预确认 | **要**。14:30 盘中预确认（标注"预"），15:00 收盘正式确认 |
| 4 | 集合竞价 | snapshot **9:26 更新** → 9:26~9:30 开盘窗口用集合竞价价做 gap 判定（与回测用 D1 open 口径一致），通过→"买入"，不通过→直接淘汰出组 |
| 5 | UI | 组切换下拉加一个组 **`龙回头Pro`**；每张股票卡后加状态 label：**买入(深绿)/持仓(蓝)/卖出(红)**，hover 显示信号明细 |
| 6 | 数据归属 | **读写删全由策略自动完成**；qd_watchlist 允许加字段+改 UNIQUE 约束 |

## 1. 重新评估：为什么真分组优于此前的"虚拟组"方案

用户授权改表后重新权衡：

| 维度 | 虚拟组（v0.9） | 真分组（v1.1，采纳） |
|---|---|---|
| 前端改动 | 改 watchlistGroups/visibleWatchlist 计算逻辑+独立渲染分支 | 组自动出现在下拉（group_name 机制天然支持），只加状态 label 行 |
| 价格显示 | 接口自带快照价 | **免费**：现有 watchlistPrices 轮询直接覆盖 |
| 点击/加仓/监视任务 | 需要适配 | **复用现有 hover 快捷按钮与点击开图** |
| 与用户手工自选的关系 | 零干扰 | 同股票可同时在两个组（改约束后），互不干扰 |
| 代价 | API 特判、渲染分支 | 一次迁移：改约束+3处 ON CONFLICT 目标+前端 key 修复 |

**结论：采纳真分组。** 前提是完成 §3 的表改造（用户已授权）。

原 UNIQUE 约束的问题：`UNIQUE(user_id, market, symbol)` 使同一股票全库只能属一个分组，且 `market.py` add 接口 UPSERT 会改写 group_name——引擎写"龙回头Pro"组会把用户手工自选里的同票搬走。改约束后此问题消失。

## 2. 存储设计：双层结构

### 2.1 `qd_dragon_signals`（事实层，新建）

全量状态机 + 历史留存（复盘/对拍/买卖点标记的数据源）。结构同 v0.9 草案：trade_date/strategy/code/board/entry_style/score/state(状态机)/signal·confirm·entry·exit 各字段 + UNIQUE(trade_date, code, entry_style)。**它是唯一事实源**。

### 2.2 `qd_watchlist`（展示层，改造）

策略组行 = 活跃信号的"投影"，由引擎每轮全量对账维护：

```sql
-- 迁移 (dragon_store.py ensure 内幂等执行)
ALTER TABLE qd_watchlist
    ADD COLUMN IF NOT EXISTS strategy_state  VARCHAR(20),   -- NULL=普通自选; 观察中/买入/持仓/卖出
    ADD COLUMN IF NOT EXISTS strategy_detail JSONB;          -- hover明细: {entry_style,score,signal_date,
                                                             --   signal_price,entry_price,stop_price,
                                                             --   d1_chg,d1_vol_r,exit_reason,lu_date,pullback_days,pre_confirm}
ALTER TABLE qd_watchlist DROP CONSTRAINT qd_watchlist_user_id_market_symbol_key;
ALTER TABLE qd_watchlist
    ADD CONSTRAINT qd_watchlist_ukey UNIQUE (user_id, market, symbol, group_name);
```

- `strategy_state IS NULL` = 普通手工自选行，一切现有行为不变
- 策略组行：`group_name='龙回头Pro'`, strategy_state ∈ {观察中, 买入, 持仓, 卖出}
- 失效（弱确认/开盘gap放弃/出场执行完毕）→ **行直接删除**（"删除失效股票"），历史仍在 signals 表

### 2.3 兼容性影响面（已逐个核查）

| 消费者 | 影响 | 处理 |
|---|---|---|
| `routes/market.py:364` add 接口 | ON CONFLICT 目标变了 | 改 `ON CONFLICT (user_id, market, symbol, group_name)`，UPDATE SET 保留 name/group_name 语义 |
| `services/indicator_review.py:701` | 同上 | 改 ON CONFLICT 目标 |
| `services/user_service.py:45` | `ON CONFLICT(...) DO NOTHING` | 改目标（保持显式） |
| `services/portfolio_monitor.py:1298` | SELECT 可能返回同票两行（不同组） | 实施时读完整查询，按需去重 |
| `routes/market.py` rename/remove-group | 按组操作 | 无影响（策略组不给改名/删组入口） |
| 现网表结构 | 已实测 = init.sql 版本（8列，无 industry/concept/new_price/change_rate），group_name NOT NULL DEFAULT '默认自选' ✓ | xuangu.py 的 watchlist INSERT 引用了不存在的列，属存量问题，不在本次范围 |
| `init.sql` | 新部署一致性 | 同步更新 DDL |

## 3. 状态机与组内生命周期（d1open，单组）

```
D0 16:30 dragon_scan: 全市场判定 → watch_pending (signals表)
  → [不入组]（见 §4 观察票处理）

D1 09:26 dragon_monitor 开盘窗口: 用9:26快照(集合竞价价)算gap
  ├─ gap ∈ 区间[(a)-3,+2 / (b)-3.5,+5.5] → state=买入 → 入组(label 买入·深绿)
  └─ 否则 → expired → 不入组/出组                                    [淘汰①]

D1 盘中: monitor 持仓检测(止损/追踪/巨量/断板) → state=卖出 → label 卖出·红  [出场]
D1 14:30: 预确认(extra.pre_confirm='强'/'弱', label 加"预"角标)
D1 15:00 正式确认:
  ├─ 强/中 → state=持仓 → label 持仓·蓝
  └─ 弱   → state=卖出(D2开盘清仓) → label 卖出·红
D2 09:30后首tick: 卖出执行完毕 → state=closed → **从组里删除行**           [淘汰②]
持有期: 到期/逃顶/断板/巨量 → 卖出 → 执行 → 删行
```

组内任意时刻 = 观察中(可选) + 买入 + 持仓 + 卖出 的活跃集，日均 15~25 只，不重。

## 4. 观察票（watch_pending）的处理 —— 默认方案

- **默认：不入组**。d1open 模式下"买入"label 在 9:26 判定后才出现，此时才真正可操作；观察票（日均 30~40 只）不进组，避免"100多只显示偏重"。
- 提前观察的补偿：today 接口仍返回观察池全量明细，**后续如需展示**，可加第二个组"龙回头Pro·观察"或组内折叠区（一行配置，前端无需新结构）。
- ⚠️ 此项是本轮唯一未完全拍板的点：若希望观察票也进组（label"观察中"灰色置底），改动极小，说一声即可。

## 5. 后端实施（`app/market_cn/auto/` 包，单一事实源）

```
auto/
├── dragon_core.py    # 从 test_dragon.py 提取: DRAGON2_PARAMS(自动化固定 entry_mode='d1open')、
│                     #   dragon2_today_d0_signals、_dragon2_d1_confirm、run_backtest_dragon2、均线工具 (零IO零print)
├── dragon_store.py   # qd_dragon_signals DDL+CRUD; qd_watchlist 迁移(加列+改约束)幂等ensure;
│                     #   sync_watchlist_group(): 全量对账写"龙回头Pro"组 (UPSERT/DELETE, 用新约束)
├── dragon_scan.py    # 16:30 盘后扫描: 全市场1D → 判定 → signals表 (仿 post_market_batch 数据就绪检测)
├── dragon_monitor.py # 盘中60s: 快照新鲜度闸门(>90s跳过) → 9:26开盘窗口gap判定 → 14:30预确认
│                     #   → 15:00确认 → 持仓出场检测 → 更新signals + sync组 (复用 realtime_monitor.py 的分时/VWAP重构)
└── dragon_api.py     # GET /api/market/dragon/today (分层列表+组内状态), /markers (B/S点)
```

scheduler 挂载（复用 Task 机制，不自建线程）：

```python
Task("dragon_scan",    _dragon_scan,    interval=86400, trading_only=False,
     once_per_day=True, trigger_hour=16, trigger_minute=30),
Task("dragon_monitor", _dragon_monitor, interval=60, trading_only=True),
```

**test_dragon.py 改为 import dragon_core**（CLI 行为不变），提取后重跑 300/120 日回测与基线对数（1627/2437 笔、强确认 86.9% 等）验证零漂移。

## 6. 前端实施（WatchlistPanel/index.vue + KlineChart.vue）

| 文件 | 改动 |
|---|---|
| `api/market.js` | + `getDragonToday()` / `getDragonMarkers()` |
| `WatchlistPanel/index.vue` | ① 卡片行加状态 label：`v-if="stock.strategy_state"` 渲染 `<span class="wl-strategy-tag" :class="...">`，买入=深绿/持仓=蓝/卖出=红，外层 `a-tooltip` 绑 `strategy_detail` 明细（状态/形态/score/信号价/买点价/止损价/确认数据/出场原因/预确认角标）；② `getWatchlist` 返回需带两个新字段（后端 SELECT 加列）；③ **`:key` 修复**：`wl-${market}-${symbol}` → 加 group_name（同票双组防 key 冲突）；④ 组内卡片按状态排序（卖出>持仓>买入>观察中）；⑤ hover 删除按钮对策略组行隐藏（读写删归策略，防"删了又回来"）；⑥ `loadWatchlistPrices` 对同票双组去重传参 |
| `KlineChart.vue` | + `markers` prop → klinecharts 9.8 overlay B/S 点（日线/分时通用）+ 分时入场价横线 |

标注细节：14:30 预确认在 label 右上加"预"小角标（pre_confirm 字段），15:00 正式确认覆盖。

## 7. 验证与灰度

1. dragon_core 提取 → 回测对数（300/120 日基线）
2. 约束迁移在本地 PG 先执行 + 三处 ON CONFLICT 回归（手工自选增删改/收藏/FTUE seed）
3. **空跑一天对拍**：自动化信号 vs `test_dragon.py --today` 人工流程（2026-09-04 周四），一致后切自动
4. 前端：默认自选组回归（增删改/改名/删组/价格轮询）+ 策略组新行为

## 8. 残留风险

- 除权价格断层（603269/688800 案例）：影响 markers 与收益展示，monitor 判定影响小（gap 判定在集合竞价日，断层日≠判定日）；后续加"隔夜 gap 超涨跌停 → 除权修正"
- 策略组行被其它代码误改（现无，迁移后约束保证同票异组共存）
- 单轮 monitor 超时：running 标志自动跳过下个 tick，最坏延迟 60s
