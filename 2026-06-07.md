# 2026-06-07 工作日志 — 分项记分制决策系统

## 概述
将 Agent 从"LLM自由推理"架构重构为"分项记分制+闭环迭代"架构。核心改动：新建 `chain/` 模块（6个文件）+ `decision_evaluation.sql`（5张表）+ 重构在线评估器。

## 设计原则
1. **缺失≠0** — 缺失项从权重池剔除，剩余项归一化，不污染总分
2. **每个工具/步骤可验证可迭代** — T+N 实际涨跌回验，因子权重自动衰减调整
3. **标准化输出** — DecisionCard 结构化输出，缺失组件必须在 gaps 中显式提示
4. **目标是直接决策** — 输出 BUY/SELL/HOLD/SKIP，不是分析报告
5. **渐进门控** — 样本量不足时分数向50收缩，避免低样本量极端判断

---

## 一、新建文件

### 1. `backend_api_python/app/agent/chain/` — 链路决策系统（6个文件）

#### `__init__.py`
- 模块入口，导出核心类

#### `schema.py` — 数据结构定义
- **StepStatus**: ok/missing/failed/skipped/veto
- **Action**: buy/sell/hold/skip
- **Confidence**: high/medium/low/reject
- **Direction**: bullish/bearish/neutral
- **FactorScore**: 单因子评分（name/value/score/status）
- **StepOutput**: 步骤标准化输出（direction/confidence/score/signal/factors/tools_called）
- **Coverage**: 覆盖度指标（valid/missing/failed/veto，ratio 属性）
- **BreakdownItem**: 决策卡分项明细
- **Gap**: 缺失项描述（what/why/impact）
- **Blockers**: 三重阻断器（has_veto / coverage_too_low / sample_too_low）
- **Decision**: 最终决策（execute/action/reason/fallback_action）
- **DecisionCard**: 完整决策卡（含 to_markdown() 输出）
- **常量**: VETO_SCORE=-1000, COVERAGE_THRESHOLD=0.4, STEP_NAME_CN 映射

#### `skill_contract.py` — Skill 输出契约
- **SKILL_OUTPUT_TEMPLATE**: 注入到 skill instructions 末尾的 JSON 输出模板
- **parse_skill_output()**: 三策略解析（JSON块 → 关键词匹配 → 兜底neutral）
- **parse_tool_details()**: 从 agent 输出提取工具调用列表
- 规则：score 0-100（50中性），confidence 基于数据充分度，缺失因子必须标 status:"missing"

#### `chains.py` — 链路定义
- **ChainStep**: name/agent/order/required/description
- **ChainDef**: chain_id/name/steps/trigger_verbs/trigger_nouns
- **内置链路**:
  - `evaluate+stock`: 13步（政策→游资→解禁→概念→动量→情报→技术→指标→选股→资金→回测→多头→空头）
  - `screen+stock`: 3步（条件选股→指标验证→情报过滤）
  - `scan+market`: 3步（大盘指数→涨停池→资金流向）
- **触发**: `get_chain_for_intent(verb, noun)` 精确/verb/noun 三级匹配

#### `executor.py` — 链路执行器
- **ChainExecutor**: 按链路定义依次调度子 Agent
- **_execute_step()**: 构造消息 → 调用子Agent → parse_skill_output() → 判断状态
- **_build_decision_card()**: 核心决策卡构建
  - 分项打分（score × weight）
  - 覆盖度计算（valid_steps / total_steps）
  - 加权评分（缺失项从权重池剔除，剩余归一化）
  - **渐进门控**: `_sample_confidence(evaluated_count)` — 对数增长 0.0→1.0
    - 0条→0.0（分数固定50），5条→0.5，10条→0.7，20条→0.85，50条+→1.0
    - adjusted_score = 50 + (weighted_score - 50) × confidence_mult
  - 三重阻断：veto→SKIP / coverage<40%→HOLD / 无数据→HOLD
  - 多空冲突检测（bull/bear 各占40%-60%→conflict→HOLD）
- **_load_step_weights()**: 从 evaluator 读取历史权重（step_weight 和 factor_weight 取max）
- **_save_to_db()**: UPSERT 到 qd_agent_decisions + qd_agent_decision_steps

#### `evaluator.py` — 闭环评估器
- **evaluate_pending()**: 获取 T+1/3/5 实际涨跌，写入 qd_agent_decision_results
- **update_factor_weights()**: 带因子级时间衰减聚合准确率
  - 因子类型→半衰期映射：政策/消息7天，游资/资金14天，概念/板块21天，技术指标30天，量价60天
  - 衰减公式: weight = 0.5 ^ (days_ago / factor_half_life)
  - 自动推断因子半衰期（`_get_factor_half_life()`）
- **update_tool_eval()**: 统计工具调用成功率/有用率
- **get_weight_adjustments()**: 生成权重调整建议（acc_3d≥65%→加权1.3，≤35%→降权0.5）
- **walk_forward_validate()**: 滚动验证（train_days→test_days）
- **get_eval_report()**: 综合评估报告
- **auto_evaluate_and_update()**: 每日盘后自动闭环（评估→权重→工具→建议）
- **get_chain_eval_stats()**: 门控统计（≥10条已评估才允许 BUY/SELL）
- **start_chain_eval_worker()**: 后台线程，每4小时运行，指数退避（4h→8h→16h→24h）
- **方向判定阈值**: 0.3%（旧0.5%太高，震荡市丢弃过多样本）
- **中性结果处理**: actual==neutral 仍参与计分（旧逻辑丢弃，震荡市样本流失）

### 2. `backend_api_python/migrations/decision_evaluation.sql` — 数据库表（5张）

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| `qd_agent_decisions` | 决策主记录 | exec_date, stock_code, chain_id, action, score, coverage, confidence, decision_card(JSONB) |
| `qd_agent_decision_steps` | 步骤详情 | decision_id, step_name, status, direction, score, signal, factors(JSONB), tools_called(JSONB) |
| `qd_agent_decision_results` | T+N 验证 | decision_id, actual_return_1d/3d/5d, correct_1d/3d/5d |
| `qd_agent_factor_weights` | 因子权重 | chain_id, factor_name, weight, accuracy_1d/3d/5d, sample_count, decay_half_life |
| `qd_agent_tool_eval` | 工具评估 | tool_name, chain_id, calls, successes, useful_count, avg_latency_ms |

### 3. `backend_api_python/app/agent/evaluator.py` — 在线执行质量评估
- 每次 agent.run() 后立即执行（<1ms，无LLM调用）
- 三个领域评估器：
  - **coding**: 工具成功率 + 代码验证(lint/diagnostics) + 工作流(读→改) + final_answer + 错误恢复
  - **finance/trading**: 工具成功率 + 数据获取完整性 + 分析链路完整度 + final_answer + 实际市场数据
  - **generic**: 工具成功率 + 工具链遵循度 + 步骤效率 + final_answer + 实际数据
- **闭环动作**: success→写回tool_chains.json / failure→记录tool_chain_failures.json / grey→不操作

### 4. `backend_api_python/app/services/experiment/scoring.py` — 策略评分服务
- 多因子加权评分（return/annual_return/sharpe/profit_factor/win_rate/drawdown/stability）
- 等级: A(≥85) / B(≥72) / C(≥60) / D(≥45) / E
- 市场环境适配（bull_trend/range_compression/high_volatility）
- 样本量惩罚（<5笔-12分，<12笔-5分）

---

## 二、数据流全景

```
用户请求 → agent._try_chain(verb, noun)
  → ChainExecutor.execute()
    → 13步子Agent依次执行
    → parse_skill_output() → StepOutput
    → _build_decision_card() → DecisionCard
      → 分项打分 → 覆盖度 → 渐进门控 → 三重阻断 → 方向判断
    → _save_to_db() → qd_agent_decisions + qd_agent_decision_steps
  → 返回 DecisionCard（BUY/SELL/HOLD/SKIP + breakdown + gaps + human_note）

[盘后定时任务]
  → evaluate_pending() → 获取T+1/3/5实际涨跌 → qd_agent_decision_results
  → update_factor_weights() → 带衰减聚合因子准确率 → qd_agent_factor_weights
  → update_tool_eval() → 工具成功率/有用率 → qd_agent_tool_eval
  → get_weight_adjustments() → 权重调整建议（日志记录）

[下次执行]
  → _load_step_weights() → 从 evaluator 读取新权重
  → 权重自动迭代生效
```

---

## 三、待办
- [ ] 补丁包打包（quantdinger-patch-20260607.tar.gz）
- [ ] 回测验证分项记分制的实际效果
- [ ] 前端 DecisionCard 可视化
- [ ] 因子权重自动应用（目前仅建议，需人工审核或配置自动应用）
- [ ] 验证系统改为按实际出场规则（止盈/止损/最大持有天数）替代固定 T+1/3/5 <==还是有bug,还是应该以准确率来评定,应该以止盈/止损/转变的时间加上盈亏率来评定.A股只能做多,单位时间内赚得越多,正确率越高,分数自然应该高.

---

## 2026-06-08 改动 — 子项级验证

### 问题
原系统用决策级正确率（`r.correct_3d`）驱动因子权重更新，与"分项独立打分"的设计哲学矛盾。
例：技术面看多(正确)+游资看空(错误)，决策整体看多(正确)→两个因子都加分，游资判错了也拿到正权重。

### 改动
1. **`qd_agent_decision_steps` 表加字段**：`actual_direction`、`step_correct`、`calibration_factor`
2. **`_evaluate_single()`**：每个步骤独立跟实际方向对比，写回 `step_correct`；基于 score 计算校准因子
3. **`update_factor_weights()`**：用 `ds.step_correct` 替代 `r.correct_3d`，校准因子微调衰减权重（±5%）
4. **`update_tool_eval()`**：同步改用 `ds.step_correct`（工具无 score，不涉及校准因子）

### 校准因子
score 越偏离50（越自信），权重更新幅度越大。最大 ±5%。
- `calibration_factor = 1 + |score-50|/50 × 0.05`
- score=100 → 1.05，score=80 → 1.03，score=60 → 1.01，score=50 → 1.00

### 原则
分项记分制下，每个子项的正确率远比决策整体正确率重要。谁对谁加分，谁错谁扣分。
校准因子是微调，不是主驱动力——权重的主驱动力还是长期正确率。
