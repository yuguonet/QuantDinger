# QuantDinger 量化策略优化 - 工作日志

## 日期：2026-05-20

---

## 一、环境信息

- **系统**：Windows + PowerShell
- **CPU**：40 核
- **项目路径**：`D:\QuantDinger\`
- **数据库**：PostgreSQL，已有完整数据
- **数据情况**：
  - 日线（1D）：覆盖 A 股全市场
  - 15 分钟线（15m）：~8000 条/只，2024-04 至今
- **目标**：A 股小资金快速复利

---

## 二、项目结构

- **后端**：`backend_api_python/`（Flask + PostgreSQL）
- **优化器**：`optimizer/`（核心模块）
  - `runner.py` — 主入口，CLI 参数
  - `strategy_optimizer.py` — 参数搜索（Random/Optuna + 早停）
  - `walk_forward.py` — Walk-Forward 验证（每 fold 独立优化）
  - `param_space.py` — 原始 6 个模板（build_strategy 路径，信号更宽松）
  - `strategy_templates_ashare.py` — A 股扩展 10 个模板（build_config 路径，参数保守）
  - `strategy_templates_llm.py` — LLM 生成 5 个模板
  - `strategy_templates_mine.py` — 自定义中短线 5 个模板
  - `strategy_compiler.py` — JSON config → Python 代码编译器
  - `indicator_strategy_builder.py` — IndicatorStrategy 代码生成器
  - `scoring.py` — 统一评分模块
  - `ashare_adapter.py` — A 股交易规则适配（T+1、涨跌停、佣金）

---

## 三、已完成测试及结果

### 测试 1：日线 + 原始模板（6个）+ 5 只股票
- **300750.SZ + macd_crossover**：Sharpe 2.47, WinRate 100%, MaxDD 0.9%, WF 测试 1.23 ✅
- 其他全部零交易或负收益

### 测试 2：日线 + mine 模板（5个）+ 5 只股票
- 22/25 零交易，trend_pullback_buy 有 3 笔但 WF 测试 -10.0

### 测试 3：15m + mine 模板（5个）+ 3 只股票
- trend_pullback_buy 有交易但全亏（Sharpe -9~-13）
- 其他零交易

### 测试 4：15m + 原始模板（6个）+ 3 只股票
- **全部负收益**，结论：15m 不适合

### 测试 5：日线 + LLM 模板 + 000001.SZ
- 零交易，早停

### 测试 6：日线 + ashare 模板（9个）+ 50 只股票
- 441 任务完成，143 有交易，**WF 测试全部为负**
- 问题：交易太少（1-3 笔），参数太保守

### 测试 7：日线 + 原始模板（6个）+ 100 只股票 + 2023-2026 ⭐ 重大突破

```powershell
python -m optimizer.runner --all --set original -m CNStock --all-local --random-sample 100 --seed 42 -tf 1D --start 2023-01-01 --trials 300 --score sharpe -j 18
```

结果：
- 总结果：576，交易 ≥ 3 笔：460，Sharpe > 1：**163 个**
- **WF 测试 > 0：1 个** 🎯

**🏆 唯一通过 WF 验证的组合：300054.SZ + kdj_crossover**
| 字段 | 值 |
|------|------|
| Sharpe | 2.97 |
| 胜率 | 84.6% |
| 最大回撤 | -3.80% |
| 总收益 | 25.70% |
| 交易次数 | 13 笔 |
| WF 测试得分 | **2.24** ✅ |

**其他高潜力候选（WF 接近 0）：**
| 股票 | 模板 | Sharpe | 胜率 | 收益 | 交易 | WF |
|------|------|--------|------|------|------|-----|
| 000636.SZ | kdj_crossover | 3.16 | 75% | 27% | 12 | -0.15 |
| 002203.SZ | kdj_crossover | 3.17 | 75% | 12.4% | 8 | -0.29 |
| 002460.SZ | kdj_crossover | 3.34 | 100% | 17.2% | 4 | 0.0 |
| 002350.SZ | macd_crossover | 3.29 | 100% | 15.1% | 3 | 0.0 |

---

## 四、关键发现

1. **日线可行，15m 不可行** — T+1 + 噪音
2. **kdj_crossover 是最有效的模板**（占 WF 前 10 中 7 个）
3. **原始模板 + sharpe 评分 + 2 年半数据** 是正确的组合
4. **300054.SZ + kdj_crossover** 是第一个 WF 测试为正的策略
5. **选标的比选策略重要** — 创业板/科创板波动大优先
6. **scoring 惩罚**：trades<3 → -10.0, avgProfit<2% → -5.0

---

## 五、下一步计划

1. 对 300054.SZ + kdj_crossover 做详细 WF 验证（看各 fold 参数稳定性）
2. 扫描更多股票（200-300 只），看有没有更多 WF > 0 的组合
3. 最终挑 2-3 个最优组合做样本外验证
4. 实盘前确认：券商佣金、滑点、流动性

---

## 六、CLI 常用命令速查

```powershell
# 查看本地数据
python -m optimizer.runner --list-local

# 跑单只股票
python -m optimizer.runner -t kdj_crossover -s "300054.SZ" -tf 1D --start 2023-01-01 --trials 300 --score sharpe

# 全市场扫描
python -m optimizer.runner --all --set original -m CNStock --all-local --random-sample 100 --seed 42 -tf 1D --start 2023-01-01 --trials 300 --score sharpe -j 18

# 清进度重跑
Remove-Item D:\QuantDinger\optimizer\optimizer_output\_progress.json -ErrorAction SilentlyContinue
```
