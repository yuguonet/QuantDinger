好思路，值得讨论。先拆清楚几个关键问题。
当前 vs 你的方案
现在：plan 是一次性的 → CodeAgent 跑到 max_steps 截断 → 直接返回
plan ──→ CodeAgent(max_steps=10) ──→ 截断 ──→ 返回给用户
你提的：plan 是多轮循环的 → CodeAgent 超步后返回中间结果 → plan 重新决策 → 下一轮 CodeAgent
          ┌──────────────────────────────────────────┐
          │                                          ▼
plan(1) ──→ CodeAgent(10步) ──→ 超步，带中间结果返回 ──→ plan(2)
                                                          │
                                                          ▼ 直到完成
                                                     plan(3) ──→ ...
需要处理的几个核心问题
1. 状态传递：中间结果放哪？
第二次 CodeAgent 是新实例，没有第一次的 memory。中间结果池需要：
- 选项 A：每次 plan 把上次 CodeAgent 的输出拼到 expanded_query/task prompt 里
- 问题：token 越来越长，容易爆上下文
- 选项 B：用外部存储（memory 或文件），传给下一次 CodeAgent 的工具去读
- 更可控，CodeAgent 按需读取，而不是全量灌入
2. plan 能看到什么？
第二次 plan 需要知道：
{"已完成的": ["analyze_trend(000001)", "get_sector_flow(新能源)"],
 "已完成的结果摘要": "...",
 "未完成的": "news_search(000001)",
 "剩余步数": "全局还有 15 步"}
不然 plan 不知道下一步该选什么工具。
3. 全局终止条件
全局最大 plan 轮次: 3 次（防止死循环）
全局最大工具步数: 30 步（单次 CodeAgent 10 步 × 3 轮）
任一个达到就截断。
4. 和现有 _plan() 的兼容
当前 _plan() 返回 (selected_tools, expanded_query)，其中 expanded_query 是静态扩展后的用户问题。如果要支持多轮，plan 的 prompt 里需要注入上一轮的执行摘要。
这个设计真正解决的问题
场景	当前行为	多轮 plan 的好处
跨域多步骤（趋势 + 新闻 + 板块）	一次选完所有工具，CodeAgent 10步内跑完	第一轮做完数据采集，第二轮分析，防止超步截断
上一步结果决定下一步策略	做不到，plan 时不知道数据长什么样	plan(2) 看到 summarize 结果再决定要不要 deep dive
LLM 在复杂推理中 token 耗尽	截断后直接返回不完整内容	截断后 plan 重新组织，继续推进
你倾向的执行模式
多的我不猜了，直接问你：
1. 要不要保留目前的 plan→CodeAgent 单次架构，只是超步时加提示+重试一次？——最简单，改动最小
2. 还是做你提的多轮 plan 循环，状态通过 plan prompt 传递？——更灵活，但复杂度和 token 开销都上来了
另外确认一点：你说的"返回给 plan 接着下一轮"——是同一个 plan prompt 追加上下文再调一次 LLM，还是完全重新 plan（再走一遍工具选择流程）？
▣  Sisyphus - Ultraworker · Big Pickle · 28.9s
GitHub: zhuqn1021/Agent-template）也没有实现这个大循环吧?
+ Thought: 766ms
⚙grep_app_searchGitHub [query=max_tool_rounds, repo=zhuqn1021/Agent-template]
⚙grep_app_searchGitHub [query=class TaskAgent, repo=zhuqn1021/Agent-template]
+ Thought: 1.6s
⚙grep_app_searchGitHub [query=async def chat, repo=zhuqn1021/Agent-template, path=agents/task_agent.py]
⚙grep_app_searchGitHub [query=tool_round_limit_reached, repo=zhuqn1021/Agent-template]
+ Thought: 2.2s
对，你说的没错，那个 template 也没有这个多轮 plan 循环。
它的 TaskAgent 是传统的单次 ReAct 循环：
plan(一次性筛选工具) ──→ ReAct(思考→工具→观察→...→max_tool_rounds)
                                                          │
                                                    超步 → 记录 trace
                                                    + 直接返回当前LLM结果
它只是在超步时打了个 warning + trace 事件，然后按原路返回，没有返给 plan 重试。
所以你说的多轮 plan 方案是一个新设计，template 和你们现在的版本都没实现。你的思路是对的 —— 让 plan 变成循环控制器，而不是一次性调度器。讨论范围很清晰了，没别的问题就先这样？
▣  Sisyphus - Ultraworker · Big Pickle · 20.8s
我的想法是:使用现有的memory,只不过区分父子关系,子循环就相当于2轮,3轮循环
如果smolagents被局限了,就把smolagents ReAct的核心提出了直接嵌入项目中
这样工具的错误导致的超步数,plan可以换工具或忽略工具.只不过可能需要plan对ReAct步数进行预估.或者动态控制步数.

+ Thought: 9.8s
这个设计质量很高，我按你的逻辑推演一下，看有没有隐藏的坑。
父子 Memory 结构
你本质上是在说：
parent_memory (session 级)
├── [user] "分析000001的趋势和新闻"
├── [plan] 选工具: analyze_trend, news_search, 分配5步
├── child_memory[round=1] (CodeAgent 5步)
│   ├── analyze_trend → OK, 结果A
│   ├── news_search → 超时
│   └── max_steps 超限
├── [plan] round=1 摘要: news_search 失败
│         重新选工具: replace news_search → news_search_v2, 分配3步
├── child_memory[round=2] (CodeAgent 3步)
│   ├── news_search_v2 → OK, 结果B
│   └── 正常结束
├── [plan] 全部完成, 合成最终回答
└── [assistant] 最终回复给用户
这个模型的好处是：round=1 的 CodeAgent 结果不会丢失，即使它超步了，plan 可以按需取用已完成的部分。而且如果 round=2 又超了，还能继续 round=3。
一个需要注意的点：child_memory 的传递方式。第二次 CodeAgent 启动时，是让它通过 parent 上下文知道"第一次已经拿到了 trend 结果"，还是重跑全部？前者需要精心构造 prompt，后者浪费 token。
关于"把 smolagents ReAct 核心提出来"
这个我赞同——smolagents 的 CodeAgent 有几个地方跟你们的场景不匹配：
问题	smolagents 行为	你的场景需要
超步后返回	裸 string，可能是中间推理碎屑	结构化返回 + 已完成/未完成工具清单
错误重试	内部重试，消耗步数	让 plan 决定重试/换工具/跳过
状态暴露	memory 是内部对象	Plan 需要知道当前状态来做决策
Python 沙箱	smolagents 强依赖	如果不需要，完全可去掉
但拆出来是有成本的——ReAct 循环本身不复杂：
循环:
  LLM(思考 + 决定下个工具)
  if 最终回答 → break
  执行工具 → 追加结果到 messages
  if steps >= max → break
核心大概 100-150 行，加上工具包装和 memory 管理，总共 300-400 行。代价可控。
"plan 预估步数"是最有趣的部分
这个能做到什么程度取决于工具的可预测性：
工具类型	步数特征	预估方式
analyze_trend(codes)	确定→1步	硬编码
batch_review(tool_results)	确定→1步	硬编码
news_search(keyword, pages)	pages=3 → ~3步	参数推导
deep_research(topic)	不确定 → 5~15步	动态扩缩
可行的策略是 渐进式分配：
round=1: 给 5 步（保守）
  → 超步 → 查看已完成: "分析做完了，新闻没搜完"
  → round=2: 再给 3 步，只做新闻搜索
  → 完成
而不是一次性分配 20 步让 CodeAgent 自己跑。这样单次 CodeAgent 的 prompt 更短，LLM 不容易迷失。
真正需要讨论清楚的两个决策点
1. 拆不拆 smolagents？
- 不拆：在现有 CodeAgent 外套一层 plan 循环，超步后解析 agent.memory.steps 提取已完成的工具结果
- 拆：自己写 ReAct 循环，完全控制中间状态暴露粒度
2. Plan 的定位
- 轻量 plan：只做工具筛选 + 步数分配，不参与内容理解
- 重量 plan：plan 也能看中间结果，做分析决策（更像一种分层 agent）
你现在倾向于哪个方向？还是说先按不拆 smolagents + 轻量 plan 的方案落地看看，再决定要不要深化？
▣  Sisyphus - Ultraworker · Big Pickle · 26.5s
我想得可能更简单一些,比如10步以内能完成的就一轮循环完成了,超过了的就多轮循环.
或者还简单一些,ReAct保留最后一步作为记录工作现场,交给plan后就相当于是新一轮和上轮历史相关的对话一样.
+ Thought: 9.6s
这个思路很干净，我确认一下理解是否准确：
你说的方案
ReAct Round 1 (N步)
  → analyze_trend → 结果A
  → news_search → 超时或错误
  → max_steps 到达，停在这里
  → 保留「最后一步的上下文」—— LLM 正在思考什么、已经拿到了什么

Plan 收到这个现场:
  "趋势搞完了，新闻没拿到，因为超时"
  → 换工具 news_search_v2
  把 round 1 的上下文 + 新工具 拼进去

ReAct Round 2
  看到的是: 趋势结果A + 新闻没搜到 + 现在有 v2 工具
  不重新跑趋势，直接接着干
核心思想：不需要提取结构化中间结果，也不需要搞分层 memory，只要把上一轮的对话上下文（messages）原样传给下一轮就行。
这个在现有架构下怎么落地
你的 TaskAgent 是 plan + CodeAgent 的方式。要实现这个，关键改动只有一处：
task_agent.py L540 组装 task prompt 时，把上一轮 CodeAgent 的 完整消息历史（agent.write_memory_to_messages()）作为"背景"写进去，再加上 "前面 N 步完成了，现在继续" 的引导。
# 伪代码
round = 1
while not done:
    task = build_task(expanded_query)
    if round > 1:
        task = f"【上一轮已完成】\n{round1_summary}\n\n【继续任务】\n{task}"

    agent = SmolCodeAgent(..., max_steps=dynamic_step)
    result = agent.run(task)

    if max_steps_reached(agent):
        round += 1
        continue  # 下一轮，agent 是新实例但上下文通过 task prompt 传递
    done = True
这样 smolagents 完全不需要拆，CodeAgent 每轮是新实例，但上下文通过 prompt 接力。
两个要你确认的点
1. "保留最后一步" —— 是把 CodeAgent 的全部 memory steps 都序列化到下一轮的输入里，还是只保留最后 LLM 的一句输出？前者 token 重但信息完整，后者轻但可能漏细节。
2. 动态步数你倾向怎么控制：
- plan 先预估一个数（比如 5），超了就再给 5
- 还是固定步数（比如 10），超了就自动发起下一轮不管剩多少？
▣  Sisyphus - Ultraworker · Big Pickle · 23.5s
保留最后一步是用于做当前轮前面做过的工作的小结.保留最后一步也行,单独一个LLM也行.我不知道哪种方式更好.或者另一种方式:ReAct每做一步就做个记录到临时缓存,结束后释放.
+ Thought: 9.9s
好，我帮你把三种方式的实际差异理清楚，然后你来做选择。
三种方式的本质
方式1：保留 LLM 最后一步
[ReAct R1]
analyze_trend → 数据A
kdj_calc → 数据B
→ LLM 最后写了一句: "趋势看多，接下来查新闻"

[传给 R2]
"趋势看多，接下来查新闻" + 新工具
风险：LLM 总结可能丢细节。比如 kdj_calc 返回了顶背离，但最后一步 LLM 忘了写进去，R2 就不知道。
方式2：单独 LLM 做小结
[ReAct R1] 完成后
→ 轻量 LLM(messages=全部步骤):
  "已完成: analyze_trend→看多(85分), kdj→顶背离
   未完成: news_search → 超时
   下一步建议: 换个搜索工具继续"
好处：格式可控，内容比 LLM 最后一句更可靠
代价：多一次 LLM 调用（但是可以用便宜模型做，比如 deepseek-chat）
方式3：每步写临时缓存
[每步执行后]
cache["round_1"] = {
  "tool_results": {
    "analyze_trend": {"trend": "看多", "score": 85},
    "kdj": {"divergence": "顶背离"}
  },
  "errors": ["news_search: 超时"],
  "completed": ["analyze_trend", "kdj"],
}

[R2 启动时] 把 cache 拼到 prompt
好处：最可靠，结构化，不丢数据
代价：需要写一个 cache 中间层 + 定义数据结构
我帮你做的选择判断
实际上方式3的变体你已经有了一半——tool_round_limit_reached trace 和 agent.memory.steps。smolagents 的 write_memory_to_messages() 天然就是一个 step-by-step 日志。
我建议你先别引入新 cache 层，直接问自己两个问题：
Q1: token 贵不贵？
如果 R1 跑了 10 步，write_memory_to_messages() 每条消息包含完整 tool input/output。按你们工具的典型结果大小（analyze_trend 返回挺大一个 dict），一步可能 2k-5k tokens。10 步就是 20k-50k tokens 传给 R2。
如果你的 LLM 上下文窗口大（比如 qwen-max 128k、deepseek 1M），这点成本可以忽略。
Q2: 信息丢失能不能接受？
如果工具返回的数据在后续还有用（比如 R1 拿到了行情数据，R2 要用这些数据做进一步计算），那 LLM 小结（方式1/2）都会丢数据，只能选方式3。
如果只是用来决定"下一步做什么"，LLM 总结就够了。
所以核心判断点就是：R2 需不需要 R1 产生的原始工具结果数据？
- 不需要（只需知道做了啥+结论）→ 方式2（单独 LLM 总结），最干净
- 需要 → 方式3 或直接塞完整 memory。这时再考虑 token 成本能不能接受
▣  Sisyphus - Ultraworker · Big Pickle · 24.2s
缓存是扁平方式的,让agent自己写代码去读缓存中内容参与计算?
+ Thought: 9.4s
这个方向很有意思，而且跟你现有的 smolagents CodeAgent 配合得天衣无缝。
之前 vs 这个方案的范式转换
 	之前的方式	这个方案
数据流向	framework 拼 prompt → 灌给 LLM	agent 主动拉
谁决定需要什么	plan 或框架代码	agent 自己（写 Python 读 cache）
跨轮数据传递	prompt 接力	cache 共享
框架复杂度	高（组装/截断/摘要）	低（只写不读）
核心变化是：*框架从"喂数据的人"变成"放数据的人"*，agent 饿了自己去拿。
在你的架构里落地是什么样
TaskAgent 级:
  cache = {}   ← 扁平 dict，存活于 TaskAgent 生命周期

[Tools]                             [CodeAgent R1]
analyze_trend(000001)                 Python:
  → 返回 {"trend": "看多", "score": 85}   trend = analyze_trend(codes="000001")
  → 自动写入:                             # → framework 自动写入
    cache["analyze_trend_000001"]            cache["analyze_trend_000001"]
                                         kdj = kdj_calc(...)
                                         # → 自动写入
                                         cache["kdj_000001"]
                                         还没查新闻，步数不够了
                                         result = "步数不够，新闻没查"

[TaskAgent 检查超步] → [CodeAgent R2]
  cache 还在（R1 写进去的全在）
  R2 启动时得到一个工具: read_cache(key)
  
  Python:
    trend = read_cache("analyze_trend_000001")
    kdj = read_cache("kdj_000001")
    # 直接用 R1 的数据继续，不用重新跑
    news = news_search("000001", ...)
注意：R2 不知道 R1 做了什么。它只看 cache 里有什么。这就是你想要的"扁平"——没有轮次概念，只有数据是否可用。
对你现有代码的改动量
最小改动方案：加一个 read_cache 工具 + 自动写 cache
Step 1：在 ToolRegistry 级别加自动缓存层 — 所有工具的 safe_execute 结果自动写入 cache
# registry.py 的 call() 里加一行
async def call(self, name, **kwargs):
    result = await tool.safe_execute(**kwargs)
    if result.success:
        _global_cache[f"{name}_{kwargs.get('codes', '')}"] = result.output
    return result
Step 2：注册 read_cache(key) 工具，让 CodeAgent 能读
def read_cache(key: str) -> Any:
    """读取之前工具执行的结果缓存。key 格式: '{tool_name}_{codes}'"""
    return _global_cache.get(key, {"error": f"key '{key}' 不存在"})
Step 3：跨轮循环时，cache 不清空，新 CodeAgent 通过 read_cache 按需读取
总改动：~30 行
需要确认的决策
1. cache key 命名规范 — 你倾向于 {tool_name}_{codes} 这种约定让 agent 自己猜，还是给 agent 一个 list_cache() 工具让它先看再读？
2. cache 生命周期 — 单次对话结束释放（跟随 TaskAgent）/ 还是持久化到磁盘供复盘用？
3. 需不需要 cache 写操作 — agent 能否主动写 cache["xxx"] = value，还是只能由工具自动写？
▣  Sisyphus - Ultraworker · Big Pickle · 27.0s
1.肯定不能让agent猜.先看后读也可以,索引点注入到上下文也行
2.结果出来了cache就释放了呀.
3.这个没限制吧?怎么方便和省事怎么来.
+ Thought: 9.7s
理解了。那设计就清晰了：
最终形态
TaskAgent 生命周期内:
  cache = {}    ← 扁平的临时缓存

工具执行 → 自动写 cache（registry 层截获）
                                  ┌──────────────────┐
                                  │  cache index     │
                                  │  trend_000001    │ ← 注入到 CodeAgent 上下文
                                  │  kdj_000001      │
                                  │  ...             │
                                  └──────────────────┘
                                        ↓ agent 看到 index，决定读哪些
                                   read_cache("trend_000001")
                                        ↓
                                   拿到数据，继续计算
涉及的改动（纯讨论，不动手）
1. registry.py 的 call() 加自动缓存 ~5行
async def call(self, name, **kwargs):
    cache_key = make_cache_key(name, kwargs)  # 规则: {name}_{codes}_{...主要参数}
    result = await tool.safe_execute(**kwargs)
    if result.success:
        _round_cache[cache_key] = result.output
    return result
2. 加一个 read_cache 内置工具 ~20行
不管 plan 选了哪些工具，CodeAgent 永远有这个工具，用于读取之前步骤的结果。
3. TaskAgent 跨轮时做两件事 ~15行
- CodeAgent 超步/完成后，把 cache index 注入到下一轮的上下文
- 不清空 cache，新的 CodeAgent 通过 read_cache 读取
最终用户视角
第一轮 (5步):
  trend = analyze_trend("000001")         # → 自动写 cache["trend_000001"]
  kdj = kdj_calc("000001")                # → 自动写 cache["kdj_000001"]
  "步数不够了，新闻没查"
       ↓ 超步，通知 plan
       ↓

第二轮 (3步):
  [上下文里有个索引: trend_000001, kdj_000001]
  trend = read_cache("trend_000001")      # 秒回，不重新调工具
  kdj = read_cache("kdj_000001")
  news = news_search("000001 latest")
  "趋势看多，KDJ顶背离，新闻中性，综合..."
你还有没有想讨论的点？
看起来整体设计已经比较收敛了，核心就剩两个细节你确认下：
1. cache key 生成——按参数类型自动拼（{name}_{codes}），还是你有一套自己的规则想让工具函数自己标注？
2. cache index 注入时机——只在跨轮时才注入，还是每轮 CodeAgent 启动时都注入（让 agent 知道之前有没有人写过数据）？
▣  Sisyphus - Ultraworker · Big Pickle · 21.9s
这个cache形式是内存cache,只服务多轮循环中.虽然单轮循环也会启动,但单轮循环作用有限.生命周期是用户消息->结果输出
+ Thought: 1.5s
明白，确认一下：
[用户消息]
   │
   ▼
TaskAgent.chat()
  ├── cache = {}          ← 创建
  ├── round 1 (CodeAgent) → 自动写 cache
  ├── 超步?
  │     ├── 否 → 返回结果，cache 释放
  │     └── 是 → round 2 (CodeAgent) → 通过 read_cache 读 + 继续写
  └── 最终结果，cache 释放
生命周期很清晰：chat() 进来创建，chat() 返回释放，跟用户消息 1:1 绑定。
单轮循环里 cache 也在，意义不大但也不亏——agent 如果写 Python 代码在同一轮内复用数据，比反复调 analyze_trend 快一个数量级（内存读 vs 数据源请求），算锦上添花。
今天的讨论到此收尾了？ 总结下来这轮确定了几个关键决策：
项目	结论
output 参数问题	暂不处理
超步后行为	多轮 plan 循环，不做截断返回
smolagents 内核	暂时不拆，先在外层套循环
数据传递	flat cache + read_cache，agent 主动读
cache 生命周期	单次 chat() 内，用完即释
cache index	注入到 CodeAgent 上下文让 agent 可见
这些可以写到 .omo/ 下做架构记录，下次需要实现时直接拿来用。需要我写一个？
▣  Sisyphus - Ultraworker · Big Pickle · 10.1s
output参数问题不记录,剩下的写成架构记录,尽量详细一些.因为第一步可能要把smolagents拆出来.才能做这个多轮循环.
+ Thought: 1.5s
→Read .omo 
+ Thought: 568ms
$ Get-ChildItem -Path "D:\QuantDinger\backend_api_python\app\agent\.omo" -Recurse | Select-Object FullName
FullName                                                                                             
--------                                                                                             
D:\QuantDinger\backend_api_python\app\agent\.omo\run-continuation                                    
D:\QuantDinger\backend_api_python\app\agent\.omo\run-continuation\ses_0d584ddbbffe2kmTE6MbrtincG.json
D:\QuantDinger\backend_api_python\app\agent\.omo\run-continuation\ses_0d7aa0c26ffebyuEIwn0bYDXQv.json
D:\QuantDinger\backend_api_python\app\agent\.omo\run-continuation\ses_0db06436fffetQAbNXoBYNa7qP.json
⚙invalid [tool=glob, error=Model tried to call unavailable tool 'glob'. Available tools: background_cancel, background_output, bash, compress, context7_query-docs, context7_resolve-library-id, edit, grep_app_searchGitHub, invalid, list_mcp_resource_templates, list_mcp_resources, look_at, lsp_diagnostics, lsp_find_references, lsp_goto_definition, lsp_prepare_rename, lsp_rename, lsp_status, lsp_symbols, question, read, read_mcp_resource, search_session_logs, session_info, session_list, session_read, session_search, skill, skill_mcp, task, todowrite, webfetch, websearch, websearch_web_search_exa, write.]
+ Thought: 1.6s
# Wrote .omo\architecture.md
# 多轮 ReAct 循环架构设计

## 1. 背景与动机

### 1.1 当前问题

现有 `TaskAgent` = `plan(一次性工具筛选)` → `CodeAgent(max_steps=10)` → `返回结果`。

当任务复杂（如：趋势分析 + 新闻搜索 + 板块分析 + 综合研判），10 步很容易耗尽。超步后 smolagents 直接截断，返回不完整结果，没有重试或接力机制。

### 1.2 核心解决思路

**多轮 plan→ReAct 循环**：ReAct 超步后不截断结束，而是返回给 plan 做下一轮调度。每轮 ReAct 共享一个扁平的内存缓存，通过 `read_cache` 工具按需读取前轮结果。

### 1.3 关键设计原则

- **框架不替 agent 做决策**——不自动拼凑上下文，不自动摘要，让 agent 自己决定读什么数据
- **扁平缓存**——没有轮次、层级概念，只有 key-value
- **就近释放**——cache 生命周期 = 单次用户请求，用完即释

---

## 2. 整体架构

```
┌─────────────────────────────────────────────────────────┐
│ TaskAgent.chat()                                        │
│                                                         │
│  cache = {}   ← 用户消息级缓存                           │
│                                                         │
│  ┌─ while not done ─────────────────────────────────┐   │
│  │                                                   │   │
│  │  Plan:  根据意图/上轮结果选工具 + 分配步数         │   │
│  │                                                   │   │
│  │  ReAct: 执行工具 → 自动写 cache                   │   │
│  │         通过 read_cache 读前轮结果                  │   │
│  │         通过上下文中的 cache index 知道有什么数据   │   │
│  │                                                   │   │
│  │  超步? ──→ 记录本轮摘要 → 继续循环                │   │
│  │  完成? ──→ 返回最终结果                            │   │
│  │                                                   │   │
│  └───────────────────────────────────────────────────┘   │
│                                                         │
│  return response  → cache 释放                          │
└─────────────────────────────────────────────────────────┘
```

### 2.1 关键变更：提取 ReAct 核心

smolagents CodeAgent 的 ReAct 循环无法满足需求，原因：

| 问题 | smolagents 行为 | 需要的 |
|---|---|---|
| 超步后返回 | 裸 string，可能是中间推理碎屑 | 结构化 cache + 可见的已完成/未完成 |
| 状态暴露 | memory 是内部对象，外部难访问 | Plan 需要知道当前执行状态 |
| Python 沙箱 | 强依赖 `additional_authorized_imports` | 不需要沙箱，直接调工具 |
| 重试策略 | 内部自动重试，消耗步数 | 让 plan 决定重试/换工具/跳过 |

**决策**：将 ReAct 核心循环从 smolagents 中提取出来， embedding 到项目内，完全控制循环粒度。

---

## 3. ReAct 核心模块

### 3.1 位置

```
tools/
  react/
    __init__.py          # 导出 ReactEngine
    engine.py            # ReAct 循环引擎
    tool_adapter.py      # 工具包装（将 Tool → ReAct 可用格式）
    cache.py             # 内存缓存
    summary.py           # 步骤摘要（可选）
```

### 3.2 接口

```python
class ReactEngine:
    """ReAct 循环引擎，替代 smolagents CodeAgent"""

    def __init__(
        self,
        llm: LLMBase,                    # 模型实例
        tools: list[Tool],               # 本轮可用工具
        cache: RoundCache,               # 全局缓存引用
        system_prompt: str,              # 系统提示词
        max_steps: int = 10,             # 本轮最大步数
    )

    async def run(
        self,
        task: str,                       # 本轮任务描述
        cache_index: dict[str, str],     # 前轮已有的缓存索引 {key: 简述}
    ) -> ReactResult:
        ...


@dataclass
class ReactResult:
    success: bool                        # True=正常完成 / False=超步中断
    output: str                          # LLM 最终输出（如果有）
    steps_taken: int                     # 实际执行步数
    error: str | None                    # 错误信息
    summary: str | None                  # 本轮执行摘要（超步时用）
    cache_keys_written: list[str]        # 本轮新写入的 cache key 列表
```

### 3.3 ReAct 循环伪代码

```python
async def run(self, task, cache_index) -> ReactResult:
    messages = self._build_messages(task, cache_index)

    for step in range(self.max_steps):
        response = await self.llm.generate(messages, tools=self.tool_schemas)

        if response.finish_reason == "stop":
            # LLM 给出了最终回答
            return ReactResult(success=True, output=response.content, steps_taken=step+1)

        if not response.tool_calls:
            continue  # LLM 没决定调工具，继续

        for tool_call in response.tool_calls:
            result = await self._execute_tool(tool_call)
            messages.append(self._tool_result_message(tool_call, result))
            # 工具结果已通过 registry 自动写入 cache

    # max_steps 耗尽
    summary = await self._summarize(messages)  # 可选：LLM 生成摘要
    return ReactResult(
        success=False,
        steps_taken=self.max_steps,
        summary=summary,
        cache_keys_written=list(self.cache.keys()),  # ← 本轮写的 key
    )
```

---

## 4. Cache 系统

### 4.1 数据结构

```python
# cache.py

class RoundCache:
    """扁平缓存，生命周期=单次 TaskAgent.chat()"""

    def __init__(self):
        self._data: dict[str, Any] = {}
        self._round_keys: set[str] = set()       # 本轮新增的 key

    def put(self, key: str, value: Any):
        self._data[key] = value
        self._round_keys.add(key)

    def get(self, key: str) -> Any:
        return self._data.get(key)

    def keys(self) -> list[str]:
        return list(self._data.keys())

    def index(self) -> dict[str, str]:
        """返回 {key: type_hint} 供注入到 ReAct 上下文"""
        return {
            k: self._infer_type(v)
            for k, v in self._data.items()
        }

    def flush_round_keys(self) -> list[str]:
        """取出本轮写入了哪些 key（给 ReactResult）"""
        keys = list(self._round_keys)
        self._round_keys.clear()
        return keys

    def clear(self):
        self._data.clear()
        self._round_keys.clear()
```

### 4.2 自动写入：registry 层

```python
# registry.py — ToolRegistry.call()

def __init__(self):
    ...
    self._cache: RoundCache | None = None

def bind_cache(self, cache: RoundCache):
    self._cache = cache

async def call(self, name, **kwargs):
    result = await tool.safe_execute(**kwargs)
    if self._cache and result.success:
        key = self._make_cache_key(name, kwargs)
        self._cache.put(key, result.output)
    return result

def _make_cache_key(self, name, kwargs) -> str:
    """生成扁平 cache key：{tool_name}_{主要参数值}"""
    # 取 codes / stock_code / symbol 等标识性参数
    primary = kwargs.get("codes") or kwargs.get("stock_code") or kwargs.get("symbol") or "default"
    return f"{name}_{primary}"
```

### 4.3 read_cache 工具

```python
# tools/builtin/cache_tools.py

def read_cache(key: str) -> Any:
    """读取之前工具执行的结果。
    
    Args:
        key: 缓存键名，格式 '{tool_name}_{codes}'，如 'analyze_trend_000001'
    
    返回之前工具的输出结果，或错误信息。
    可用缓存键可通过 CACHE_INDEX 查看。
    """
    cache = _get_current_cache()
    result = cache.get(key)
    if result is None:
        return {"error": f"缓存中不存在 key '{key}'", "available_keys": cache.keys()}
    return result


def list_cache() -> dict[str, str]:
    """列出当前可用的所有缓存条目，返回 {key: value_type} 字典。"""
    cache = _get_current_cache()
    return cache.index()
```

### 4.4 Cache Index 注入

每轮 ReAct 启动时，将当前 cache index 注入到 task prompt 中：

```
【缓存中的已有数据】
- analyze_trend_000001: dict (趋势评分+指标数据)
- kdj_000001: dict (KDJ金叉/死叉+数值)

使用 read_cache(key) 读取上述数据，无需重复调用工具。
```

---

## 5. TaskAgent 改造

### 5.1 新流程

```python
async def chat(self, user_input, session_id, use_rag=True):
    cache = RoundCache()
    self.tool_registry.bind_cache(cache)

    # 初始 plan
    selected_tools, expanded_query = await self._plan(user_input, self.llm, trace)
    if not selected_tools:
        return await super().chat(user_input, ...)

    round_num = 0
    max_rounds = 3          # 全局最大循环轮次
    max_total_steps = 30    # 全局最大总步数
    total_steps = 0
    all_summaries = []

    while round_num < max_rounds and total_steps < max_total_steps:
        round_num += 1

        # 动态分配本轮步数
        step_budget = min(10, max_total_steps - total_steps)

        # 构造 task prompt（含 cache index）
        task = self._build_task_prompt(
            expanded_query,
            round_num,
            cache.index(),
            all_summaries,
        )

        # 运行 ReAct
        engine = ReactEngine(
            llm=self.llm,
            tools=_build_react_tools(selected_tools, cache),
            cache=cache,
            max_steps=step_budget,
        )
        result = await engine.run(task, cache.index())

        total_steps += result.steps_taken
        trace.record("react_round", {
            "round": round_num,
            "steps": result.steps_taken,
            "success": result.success,
            "cache_keys": result.cache_keys_written,
        })

        if result.success:
            # 正常完成
            return AgentResponse(content=result.output, ...)

        # 超步 → plan 重新调度
        if round_num >= max_rounds or total_steps >= max_total_steps:
            break

        # 重新 plan（带上本轮摘要）
        selected_tools, expanded_query = await self._plan(
            f"{user_input}\n[前轮摘要: {result.summary}]",
            self.llm, trace,
        )
        all_summaries.append(result.summary)

    # 所有轮次耗尽，返回最后结果或错误提示
    return AgentResponse(
        content=f"任务未完全完成（已达全局步数上限）\n{result.output or ''}",
        ...
    )
```

### 5.2 Plan 多轮适配

当前 `_plan()` 的 prompt 只是静态工具列表。多轮时需要加上：

```
【上一轮执行情况】
已完成: analyze_trend(000001) ✓, kdj_calc(000001) ✓
失败: news_search(000001) → 服务超时
未尝试: sector_flow(新能源)
【当前可用缓存】
- analyze_trend_000001
- kdj_000001
```

---

## 6. 与 smolagents 解耦

### 6.1 替换清单

| 当前依赖 | 替代 | 原因 |
|---|---|---|
| `SmolCodeAgent` | `ReactEngine` | 控制超步行为、暴露中间状态 |
| `_SmolTool` | `ToolAdapter` | 简化包装层，去掉 smolagents 的 inputs/output_type 约束 |
| `_LLMAdapter` | 直接调 `LLMBase.generate()` | 去掉 smolagents 的 ChatMessage 转换层 |
| `write_memory_to_messages` | `RoundCache + summary` | 扁平缓存替代分层 memory |

### 6.2 逐步替换策略

**Phase 1（当前）**：保留 smolagents，在外部套多轮循环
- 修改 `TaskAgent.chat()` 加 while 循环
- 在 CodeAgent 外套 `RoundCache`
- 通过 ToolRegistry 注入自动缓存
- 按 `agent.write_memory_to_messages()` 提取步骤上下文传给下一轮

**Phase 2**：提取 ReAct 核心
- 新建 `tools/react/engine.py`
- 实现 `ReactEngine`，只依赖 `LLMBase.generate()` + `Tool`
- 逐步替换 smolagents CodeAgent 的调用点
- 验证全部现有工具函数兼容

**Phase 3**：清理
- 移除 `_SmolTool`、`_LLMAdapter` 等适配层
- 从依赖中移除或可选化 `smolagents`

---

## 7. 边界条件与错误处理

### 7.1 工具错误 vs ReAct 超步

两种中断需要区分处理：

```
工具执行失败:
  → registry 返回 ToolResult(success=False)
  → ReactEngine 继续循环（LLM 可以看到错误信息）
  → LLM 自行决定重试/换工具/跳过

ReAct 超步:
  → ReactEngine 达到 max_steps
  → 触发 plan 重新调度
  → plan 看到哪些工具失败了，可以换工具或跳过
```

### 7.2 全局终止保障

```python
MAX_ROUNDS = 3          # 最多 3 轮循环
MAX_TOTAL_STEPS = 30    # 最多 30 步
MAX_TOKENS_PER_ROUND = 80000  # 每轮上下文上限（防 prompt 爆炸）
```

任意一个达到 → 立即停止，返回当前已完成的 cache 内容 + 超限提示。

### 7.3 幂等性与缓存误用

`read_cache` 按 key 精确读取，不存在跨轮污染。如果前轮写了 `trend_000001`，后轮读到的就是同样的数据。如需刷新数据，调原始工具本身即可（工具调用的结果会覆盖 cache）。

---

## 8. 实现步骤（建议顺序）

### Step 1: RoundCache + 自动写缓存
- 实现 `RoundCache` 类
- registry.py `call()` 加缓存写入
- 注册 `read_cache` / `list_cache` 工具
- 验证：单轮 CodeAgent 内能读写 cache

### Step 2: TaskAgent 多轮循环（不拆 smolagents）
- `TaskAgent.chat()` 加 while 循环
- 超步后解析 `agent.memory` 获取摘要
- 构造下一轮 task prompt + cache index
- 验证：2 轮循环能接力完成

### Step 3: 提取 ReactEngine
- 新建 `tools/react/engine.py`
- 实现纯 ReAct 循环替代 CodeAgent
- 移除外层 smolagents 依赖
- 验证：全部现有工具在新引擎下行为一致

### Step 4: 清理与收尾
- 移除 `_SmolTool`, `_LLMAdapter`
- 从 `requirements.txt` 去掉 smolagents（如不再需要）

---

## 9. 附录：关键文件变更清单

| 文件 | 变更类型 | 说明 |
|---|---|---|
| `tools/registry.py` | 修改 | `call()` 加自动缓存写入、`_make_cache_key` |
| `tools/react/cache.py` | 新建 | `RoundCache` 类 |
| `tools/react/engine.py` | 新建 | `ReactEngine` ReAct 循环 |
| `tools/react/tool_adapter.py` | 新建 | 工具格式适配 |
| `tools/builtin/__init__.py` | 修改 | 注册 `read_cache`, `list_cache` |
| `agents/task_agent.py` | 重构 | 多轮循环，替换 CodeAgent → ReactEngine |
| `prompts/plan_system.txt` | 修改 | 加入上轮摘要 + cache index 上下文 |
| `requirements.txt` | 修改 | 可移除 smolagents |

+ Thought: 620ms
写好了，位置：.omo/architecture.md (file:///D:\QuantDinger\backend_api_python\app\agent\.omo\architecture.md)
分 9 个章节，核心内容：
- §2 整体架构——多轮 plan→ReAct 循环的完整流程图
- §3 ReAct 核心模块——ReactEngine 接口设计 + 循环伪代码
- §4 Cache 系统——RoundCache 数据结构 + 自动写入（registry 层）+ read_cache/list_cache 工具 + cache index 注入机制
- §5 TaskAgent 改造——多轮循环伪代码 + plan 多轮适配
- §6 与 smolagents 解耦——3 个阶段的替换策略（先外圈套循环 → 再提取 ReActEngine → 最后清理删除）
- §8 实现步骤——4 步走：先加 cache、再套多轮、再抽引擎、最后清理
比较重要的是 §6 的分阶段替换策略——你说"可能要把 smolagents 拆出来"，这个文档里给了渐进路径：Phase 1 可以不动 smolagents 先验证多轮循环的逻辑，Phase 2 再拆引擎逐个替换，风险可控。
你看看内容和结构有没有要调整的？
▣  Sisyphus - Ultraworker · Big Pickle · 1m 3s
问题:这种多轮循环方式好还是直接将ReAct动态步数好?还是直接ReAct最大步数=50的方式好?
+ Thought: 10.7s
好问题，这三个方案的取舍其实很清晰。我按几个关键维度拆开对比：
对比矩阵
维度	① 多轮 plan 循环	② 单轮动态步数	③ 单轮 max_steps=50
工程复杂度	最高	中	最低
简单任务开销	1 轮结束，和现在一样	1 轮结束，和现在一样	50 步窗口开在那里，浪费 token
复杂任务可靠性	✅ 最高	⚠️ 中	❌ 最低
错误隔离	✅ 每轮独立，坏了一轮可换策略	❌ 错误积累，中途无法纠正	❌ 一步错步步错
上下文控制	✅ 每轮 prompt 精简（cache index + 摘要）	⚠️ 逐步膨胀	❌ 50 步全塞进上下文
LLM 专注力	✅ 每轮 <10 步，聚焦	⚠️ 越长越容易迷失	❌ "lost in the middle"
模型分级	✅ plan 可用便宜模型	❌ 全程用一个模型	❌ 全程用一个模型
延迟	多轮间有 plan 调用开销	连续，最低延迟	连续，但 50 步可能很慢
关键判断
③ max_steps=50 为什么最差
不是步数多就解决问题——步数越多，LLM 越容易在长上下文中迷失。工具调用链越长，某一步出错的概率越大，而且没有外部干预机制。成本也最高（50 步全走强模型）。
一句话：50 步不是把简单问题变复杂的方案，而是把已复杂的问题变得更不可控。
② 动态步数 vs ① 多轮循环
动态步数的本质是：ReAct 自己决定还要几步。问题是 LLM 在"干到一半"时的自评估能力很差——它不知道自己还差多少，经常出现"再给我 5 步就够了"然后 5 步后又 5 步。
多轮循环多了一个外部决策者（plan），它的好处在于：
场景: news_search 连续超时 3 次

动态步数 → LLM 继续重试，再消耗 5 步，还是超时
            → 再要 5 步 → 继续超时 → 填满 50 步

多轮循环 → round 1: news_search 失败
         → plan 看到: "news_search 不可用，换个工具"
         → round 2: web_search 成功拿到结果
我的结论
多轮循环是三种方案中唯一能解决复杂任务的。动态步数和 50 步只是在现有单轮结构上打补丁，无法解决"策略错误需要外部纠正"这个根本问题。
但多轮循环也不是银弹——它的价值取决于plan 的调度质量。如果 plan 只是把同样的工具再列一次，那就退化成单轮了。
所以问题变成了：你的任务中，有多大比例需要策略切换？
- 如果大多数任务 10 步内能搞定 → 多轮循环是保险丝，99% 情况下用不到
- 如果经常有 15+ 步的复杂链 → 多轮循环是必需品
你的场景是哪一种？
▣  Sisyphus - Ultraworker · Big Pickle · 24.6s
明白了.我用它写代码,多轮循环是必须的.