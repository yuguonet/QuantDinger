# 2026-09-20

## 「明天买什么股?」故障根因定位（ts=1789835139377）

**根因：模型行为回归，非代码 bug。**
- `.env` 在 00:25:37 把 `OPENAI_MODEL` 从 `glm-5.3-flash` 换成 `deepseek-ai/DeepSeek-V4-Flash`
- 新模型 planner 只吐 `task` 一个字段，`selected_domain=""`/`tools=[]`/`phases=[]` 全丢
- → 沙箱只剩 2 个 common 工具（56 个数据工具全在 finance 域）→ 执行器按任务书调用全被判「幻觉调用」→ 空转至 max steps

**证据**：13 个成功 run 全 `glm-5.3-flash`（dom='finance'）；唯一失败 run `deepseek-ai/DeepSeek-V4-Flash`（dom=''，ctok=918）；plan_response LEN=1253 只含 task；provider 58 工具 / 56 在 finance / common 仅 2。

**修复**（`task_agent.py::_plan` 双网，仅三通道全空时触发）：
1. `_salvage_tools_from_text`：按 provider 真实注册名词边界扫描 task 正文回收点名工具
2. 域兜底：task_type∈{screen,analysis,compare,query} 且 finance 可选 → 回退 `domain='finance'`（排除 code/explain；技能模式不触发）

py_compile EXIT=0；单测 4 case 过。文档新增 §7.19。

**待办**：若要恢复原行为，`.env` 的 OPENAI_MODEL 应回 `glm-5.3-flash`（双网只兜底不消除退化）。

---

## 沙箱换真 CPython（路线 C）+ 返回结构契约改版

> 来源：会话「沙箱不断挡路排查」；时间 2026-09-20 14:00–15:00；产物 `infra/guided_executor.py` 重写、
> `tools/returns_contract.py` 重写、`nodes.py` 判官摘要同步、26 个 finance 工具补 `Returns:` 段。
> 详见 `docs/AGENT_DESIGN.md` §7.20（执行器）与 §7.13 末段（契约改版）。

**用户裁定（三条）**：① 保留代码执行、换 CPython 语义（走官方 `executor=` 扩展点自建执行器）；
② 内置函数面全量真内置（`vars(builtins)` 全给，仅覆盖 `help` 防 stdin 阻塞）；
③ 契约写在**工具描述**里、程序自动匹配，未声明的按标准处理（否定"手工维护 101 个契约"）。

**根因**：「沙箱挡路」不是 AST 白名单（白名单 09-14 已删、破坏性审批 09-18 已删），
是 `LocalPythonExecutor`（官方自实现 AST 解释器）与 CPython 的**系统性语义偏差**：
vararg 绑参错（上游 #2754 未修）、posonly/kwonly/nonlocal/生成器/with/列表推导/lambda 默认值/
dict 解包 全报错、仅 52 个内置、dunder 默认禁、错误行号指到调用点。每条偏差都长出一个补丁层。

**验收**：`tmp/qclaw/cpython_semantics_0920.py` 17/17 与裸 CPython 逐字一致（旧解释器 13/17 失败）；
`tmp/qclaw/audit_returns_contract_0920.py` 契约覆盖 19→55/118，典型阶段白名单 8 工具 → 速查段 ~1.5K 字符。

**顺带发现（已处置，见下条）**：
- `backtest_tools.run_backtest` 只 `return {"success": True}`——`summary`/`recent_trades` 算完**未回传**。
- `search_stock_intel` 旧契约文本已过期（历史写 `{items, summary}`，实际返回 `{label, composite_score, direction, veto, count, news}`）。
- `get_industry_ranking` 历史契约写 `leading_stock`，实际键是 `lead_stock`。
- `code_agent.yaml` 示例里的 `get_daily(symbol=...)` 是**不存在的工具名**（实名 `agent_get_kline`），
  会诱发幻觉调用；已改为真实工具 `get_realtime_quote` 的批量用法（规则 17 与 task_agent 注释里的同一例子也已修）。

## run_backtest 修复（2026-09-20 15:40，用户裁决"修 run_backtest"）

`tools/finance/backtest_tools.py`，**三个同源缺陷**（都是"键名/入参对不上底层契约"）：
1. **指标取数恒为 0**：按 snake_case 取 `result["total_return_pct"]` —— 该键不存在。
   真实形态是 `BacktestService._calculate_metrics` 的 **camelCase 平铺**：
   `totalReturn/annualReturn/maxDrawdown/sharpeRatio/winRate/profitFactor/totalTrades/totalProfit/totalCommission`
   （`_format_result` 平铺顶层 + `equityCurve` + `trades`）。已抽成模块常量 `_METRIC_KEYS`。
2. **算完不回传**：`summary`/`recent_trades` 组装后被丢弃（死代码）。已回传
   `{success, summary, trade_count, recent_trades}`；`equityCurve` **刻意不回传**（点数≈K线根数）。
3. **入参被静默忽略**：`stock_code`/`timeframe` 从未使用，回测实际跑策略自己配置的标的/周期
   （"回测 600519"可能跑的是 000858）。已通过 `resolver.resolve(strategy, {"symbol":..., "timeframe":...})`
   override 钩子接上（该钩子本就是为此设计的）。

同文件 `get_backtest_history` 有**同源 bug**：自己解 `result` 并按 `total_return_pct` 取（恒 0），
且与 `_hydrate_run_row` 已经抽好的行上扁平字段（`total_return/annual_return/win_rate/total_trades`）
重复 → 已删掉这段，直接读行上字段；顺手删掉因此变成未使用的 `import json`。

验收：`tmp/qclaw/verify_backtest_0920.py`（假 Service 注入同形结果）**22/22 PASS**，
覆盖指标回传/明细收敛/equityCurve 不回传/error 分支/无标准指标 note/override 生效/历史行字段。
`compileall app/agent/` EXIT=0；契约覆盖 56/118。

