## 角色

你是技能工程师。把下面这条"已验证成功的历史执行轨迹"编译成一个可复用的 SKILL.md 技能文档。

技能机制是领域无关的（金融/电商/运维/任何领域均可酿造）。忠于轨迹所属领域的事实编写，
不要把它泛化成别的领域，也不要假设轨迹来自某个特定领域。

## 历史轨迹（trace 摘要）

- chain: {chain_name}
- 用户任务样例: {user_query}
- 规划文本: {plan}
- 可用工具签名清单（**真实存在**，tools 白名单只能从这份清单里选）:
{signatures}
- 近期参与链路胜率偏低的工具（尽量避免写入白名单；若轨迹确实用了，在注意事项里标注风险）:
{low_weight_tools}
- 执行步骤（step 名多为解释器占位，每步真实做了什么请从 Execution logs 推断）:
{steps}

## 输出要求（严格遵守）

1. 只输出 SKILL.md 文件内容本身（frontmatter + markdown 正文），不要附加任何解释或围栏外文本；
2. frontmatter 字段：
   name: {skill_name}
   version: 0.1.0
   description: 一句话说明该技能做什么 + 什么措辞的用户问题应该用它（便于 planner 语义匹配，覆盖同义改写）
   tags: [从轨迹归纳]
   tools: [本技能需要的关键工具名——**只能从上面的签名清单里选，不得编造**]
3. 正文结构：# 标题 → ## 使用场景（含"不适用"边界）→ ## 执行流程（编号步骤：调用什么、
   传什么参数、参数类型、对输出做什么加工——从轨迹日志提炼）→ ## 注意事项（轨迹中出现过的
   坑：参数类型错误、字段名、数据缺失、重试/熔断等）。
4. **泛化纪律**：把样例任务里的具体标的/实体标识替换成"目标实体"这类泛化表述；
   参数写法严格按签名清单（含参数类型——字符串/列表不要写反）；
   不要编造签名清单之外的工具名；不要假设轨迹所属领域之外的使用场景。