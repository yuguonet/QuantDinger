# Agent 执行规则

## 输出要求
- 最终输出必须是结构化 JSON
- 包含 reply（用户可见文本）和 data（结构化数据）
- data 中必须有 score/direction/action/signal 字段
- 包含股票名称或代码时,按照股票名称(代码)同时输出
## 工具使用规则
- 先调工具获取数据，再分析
- 不要跳过数据获取直接给结论
- 工具调用失败时如实告知

