# 2026-05-22 工作日志（续）

## 时间：06:00 - 07:56

## 工作内容

### 1. 项目下载与上下文恢复（06:00）
- 从 GitHub 下载 QuantDinger 项目
- 读取 quick-resume.md / MEMORY.md / 2026-05-22.md 恢复上下文

### 2. 全市场验证（07:09-07:21）
- 用户已完成 `export_dragon_runs.py --min-streak=1` 导出全市场数据
- 数据范围：2026-01-06 ~ 2026-05-21（88个交易日）
- 连板分布：单板3825 + 连板905（2板574, 3板159, 4板75, 5板46, 6板39, 7板7, 8板5）
- 写了 `validate_full_market.py` 做全市场假阳性验证

#### 关键发现
- **一字板占比**：单板61个一字板, 连板96个一字板（不可买入）
- **非一字板基准浓度**：17.7%（807 multi / 4565 total）
- **最优过滤规则**（非一字板中）：
  - 涨幅≥20% + 封板≤2.8% + 上影2~8% + 波动≤10% → 39通过, 2FP, 精确94.9%
  - 涨幅≥20% + 封板≤2.8% + 振幅≥5% + 波动≤5% → 32通过, 0FP, 精确100%
- **信号高度集中**：39个信号分布在6天，其中04-28一天23个

### 3. 过滤规则优化搜索（07:21-07:28）
- 写了 `optimize_filter.py` 搜索最优规则组合
- 搜索单/双/三规则组合，按期望值排序
- 最优：上影≥3% + 前5天波动≤5% + 振幅≥5% → 24笔100%精确率
- 但数据只有2026年4.5个月，无法做年度交叉验证

### 4. 连板猎手策略 v2（07:28-07:41）
- 写了 `strategy_dragon_filter.py` 独立策略脚本
- 核心逻辑：涨停过滤（涨幅/封板/上影/波动）+ 开板出场
- 修复了3个bug：
  1. 一字板判定太严 → 改用距涨停价距离+振幅
  2. 出场引擎：涨停封板期间不应触发止盈/追踪止损
  3. 止损浮点精度 → 加0.01%容差

#### 回测结果（db_market, 2026-01~2026-05）
- 32笔交易, 81.2%胜率, 均值+14.90%, 盈亏比9.55
- 追踪止损27笔, 止盈15% 5笔
- 最大盈利+205%, 最大亏损-8.2%

### 5. IndicatorStrategy 转换（07:41-07:56）
- 在 `strategy_templates_ashare.py` 新增 `dragon_filter` 模板
- 用 `render_indicator_strategy` 生成标准 IndicatorStrategy 代码
- 注册到 ASHARE_STRATEGY_TEMPLATES，runner 可直接调用
- 经过3轮对比修正，最终 IS 与独立脚本 buy 信号 **4710组全部一致**

#### IS 与独立脚本的差异
- Buy 信号：完全一致 ✅
- Sell 信号：IS 用固定8%阈值，独立脚本用各股 threshold*0.95（实际影响极小）
- IS 优势：runner 处理 T+1、涨跌停、佣金，更真实

## 新增/修改文件

| 文件 | 说明 | 状态 |
|------|------|------|
| `optimizer/strategy_dragon_filter.py` | 独立策略脚本（横向过滤+出场引擎） | 新增 |
| `optimizer/validate_full_market.py` | 全市场假阳性验证 | 新增 |
| `optimizer/optimize_filter.py` | 过滤规则优化搜索 | 新增 |
| `optimizer/strategy_templates_ashare.py` | 新增 dragon_filter 模板 | 修改 |

## 待做

1. **扩大数据范围**：本地运行 `export_dragon_runs.py --min-streak=1 --start=2023-01-01` 获取多年数据
2. **runner 全市场回测**：`python -m optimizer.runner -t dragon_filter --all -m CNStock -tf 1D --start 2023-01-01 --end 2026-05-21 -j 4`
3. **参数优化**：让 runner 搜索最优 min_return/max_seal/min_upper/max_volatility 组合
4. **之前的待做**：大盘过滤debug + 8个策略加出场规则

## 运行命令

```bash
# 独立策略（横向过滤）
python optimizer/strategy_dragon_filter.py
python optimizer/strategy_dragon_filter.py --min-return 25 --max-seal 2.0

# IndicatorStrategy（runner 回测）
python -m optimizer.runner -t dragon_filter -s "301179.SZ" -tf 1D --start 2026-01-01 --end 2026-05-21
python -m optimizer.runner -t dragon_filter --all -m CNStock -tf 1D --start 2023-01-01 --end 2026-05-21 -j 4

# 全市场验证
python optimizer/validate_full_market.py --csv analysis_output/dragon_ohlcv.csv

# 过滤规则优化搜索
python optimizer/optimize_filter.py --csv analysis_output/dragon_ohlcv.csv
```

## 时间：08:37 - 09:26

## 工作内容

### 1. 板块分离修复（08:37-08:46）
- 发现旧策略全部信号来自创业板/科创板，主板0信号
- 原因：`min_return>=20%` 直接过滤掉主板（涨停才10%）
- 尝试归一化方案（主板×2），但封板强度无法简单归一化

### 2. 双分支架构（08:46-08:58）
- 改为两条独立分支：主板10%分支 + 创/科20%分支
- 自动检测板块，各自最优参数
- `BOARD_PARAMS = {"10pct": {...}, "20pct": {...}}`

### 3. 主板参数优化（08:58-09:26）
- 网格搜索封板/波动/量比/振幅组合
- **关键发现：量比是主板核心筛选维度**
  - 量比<1（缩量）：791笔 胜率79.5% 均值+6.71%
  - 量比>3：胜率<56%
- 最优参数：`seal≤8% + 波动≤3% + 量比≤1`
- 主板从 59.6% → **78.2%** 胜率

### 4. 横向分析（09:19-09:26）
- 用 dragon_pattern_features.csv 做板块独立横向分析
- 主板：开板日跌>5% → 胜率仅26%
- 创/科：连板期振幅小+实体比低 → 均值+27.7%（但无法在首板日单独计算）
- 创/科参数保持不变（已验证81%胜率）

## 最终结果

| 分支 | 信号 | 胜率 | 均值 | 盈亏比 |
|---|---|---|---|---|
| 主板 | 174笔 | 78.2% | +5.10% | 1.70 |
| 创/科 | 34笔 | 73.5% | +13.07% | 9.37 |
| 合计 | 208笔 | 77.4% | +6.41% | 2.36 |

## 修改文件

| 文件 | 说明 |
|---|---|
| `optimizer/strategy_dragon_filter.py` | 双分支架构 + 主板量比过滤 |
| `optimizer/strategy_templates_ashare.py` | IS模板双分支 + 量比 |
| `optimizer/test_dragon_filter.py` | 测试框架（随机数据+逐日推进） |
| `optimizer/optimize_mainboard.py` | 主板参数优化脚本 |

## 待做

1. **runner验证**：本地跑确认创/科81% + 主板78%
2. **横向分析特征入策略**：振幅/实体比需跨天计算，适合runner多日策略
3. **大盘过滤debug + 8个策略加出场规则**
